# SIASA Project-Lead Capability Matrix

> For Hermes: maintain this document after every completed serial work package. Update capability status only when there is repo evidence, verification evidence, and a committed package that materially changes stakeholder-visible functionality.

Goal: give the project lead a fast, evidence-backed view of (1) what is implemented, (2) what is only partly implemented or merely prepared, and (3) what is still materially open.

Architecture: this matrix sits above code-level details. It maps stakeholder-facing capabilities to concrete repo evidence, test evidence, and work packages so progress can be tracked as capability maturity instead of raw file churn.

Tech stack / evidence base: `docs/plans/siasa-stakeholder-fulfillment-roadmap.md`, `docs/plans/siasa-stakeholder-gap-sweep-and-development-sequence.md`, `src/siasa/**`, `tests/**`, and current governed artifacts under `build/run_artifacts/latest`.

---

## 1. How to use this matrix

### Status semantics
- `Done` = materially usable in the current product/repo baseline; verified and committed
- `Partial` = meaningful capability exists, but stakeholder intent is only partly fulfilled
- `Weak` = structural preparation exists, but practical product capability is still thin
- `Missing` = materially not implemented yet
- `Deferred by intent` = intentionally later/post-MVP unless reprioritized

### Evidence rule
A capability must not be marked `Done` based only on code structure.
It should only be marked `Done` when all three exist:
1. product/runtime evidence
2. verification evidence
3. committed package evidence

### Work-package status rule
For project steering, use these work-package states:
- `Open` = not started
- `In Progress` = active serial package right now
- `Verified` = implemented and verified locally, but not yet committed/pushed
- `Done` = verified, committed, and pushed

---

## 2. Current project-lead capability matrix

| Capability | Stakeholder focus | Current status | What exists now | What is still missing | Product / runtime evidence | Verification evidence | Last completed work package | Next planned work package |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| World overview and anomaly navigation | Fast global anomaly orientation with drill-down | Done | Interactive overview page with country drill-down links, map-facing visualization layer, and strengthened navigation controls (priority/domain/time/status filters, country search, visible-country count, and dynamic filter-result feedback) now supports operationally fast global orientation in the current governed GUI baseline | Runtime breadth can still expand later for representativeness, but the stakeholder-facing orientation/navigation workflow itself is materially usable and verification-backed | `build/local_gui/index.html`, `build/run_artifacts/latest/readmodels/world_map.json`, `src/siasa/gui/local_app.py` | GUI generation tests in `tests/unit/test_local_gui.py`, full suite pass | P1 overview navigation hardening package (status + search + visible count) | Optional later runtime-breadth expansion packages |
| Country drill-down and explanation | Understand why a country is in its current state | Done | Country profile page, explanation summary, drivers, uncertainty, gap context, links to domain pages and annotations | Richer historical comparison on country reasoning can still improve later | `countries/*.html` generation in `src/siasa/gui/local_app.py` | `tests/unit/test_local_gui.py` | Earlier country-profile GUI package | P4-WP-002 for richer comparison overlays |
| Domain deep dive and trend interpretation | Inspect domain-specific time series and drivers | Done | Domain detail pages, trend views, feature values, source context, anomaly state, analyst-friendly baseline/historical summaries, and interactive deep-dive scoping controls (feature/source filters with visible counts) now exist | Future runtime breadth expansion can still increase representativeness, but the stakeholder-facing inspect/interpret workflow itself is operationally closed in the current governed GUI baseline | `domains/*.html`, `trends.html`, `src/siasa/gui/local_app.py`, `build/local_gui/_rep_domain_deep_1` | `tests/unit/test_local_gui.py`, full suite pass, representative live probe + readiness evidence (`RUN-LIVE-REP-DOMAIN-DEEP-001`) | `P3 domain deep-dive scoping UX` current package | Optional later runtime-breadth expansion packages |
| Cross-country comparison | Compare multiple countries in one analyst flow | Done | Dedicated comparison page with coverage/confidence comparison and controls | Runtime breadth still limits practical representativeness | `comparison.html`, `src/siasa/gui/local_app.py` | `tests/unit/test_local_gui.py` | Earlier P3 comparison package | Later breadth/runtime packages |
| Source trust / coverage / gap transparency | Review source status, failures, confidence, freshness, and remediation guidance | Done | Coverage view, degraded sources, remediation watchlist, freshness overlays, source-depth summaries | Broader live-source evidence can still deepen practical value | `coverage.html`, `readmodels/system_status.json`, `src/siasa/runs/artifacts.py` | `tests/unit/test_run_artifacts.py`, `tests/unit/test_local_gui.py` | `8c3537d feat: add freshness overlays to coverage views` | Later live-source/runtime hardening |
| Validation / backtest review | Compare expected vs observed outcomes with governed evidence | Done | Validation page exists, latest governed bundle now carries both `validation_backtest` and persisted `readiness.json`, readiness exposes explicit artifact presence/absence status, the validation artifact/view now includes a multi-case portfolio summary across all governed countries in the run, a curated repo-backed reference-case library broadened to 21 countries (`UKR`, `RUS`, `CHN`, `IND`, `IRN`, `ISR`, `TUR`, `USA`, `DEU`, `EST`, `FIN`, `SAU`, `QAT`, `EGY`, `NGA`, `SDN`, `MMR`, `POL`, `PAK`, `GEO`, `TWN`) / 23 cases, historical reference-review / evidence-scoring outputs, full curated-library `provider_backed_archival_replay`, explicit archival provenance signals, richer replay-verdict-depth signals such as replay evidence score/tier plus source/provenance coverage ratios, governed partial/mismatch challenge cases with non-perfect replay outcomes, and a replay attention layer with both a detailed watchlist and an aggregated summary by attention level/reason/owner/country | Governed replay depth is now materially stronger and more realistic, but this is still repo-stored governed archival replay rather than arbitrary live historical backfill; future depth work should therefore shift from tranche-completion breadth into either broader MVP/P2 runtime expansion or richer analyst interpretation beyond the current attention-summary layer | `validation.html`, `readiness.html`, `build/run_artifacts/latest/readmodels/readiness.json`, `build/run_artifacts/latest/readmodels/validation_backtest.json`, `vmodel/verification/validation_reference_cases.yaml`, `vmodel/verification/validation_replay_inputs.yaml`, `vmodel/verification/validation_archival_replay_manifest.yaml` | Validation-related tests in repo; full suite passing; browser/runtime verification | `P1` closure slice | Reassess between broader MVP/P2 runtime breadth and the next validation depth/interpretation package |
| Traceability / lineage visibility | Follow evidence path from source to status/report | Done | Dedicated traceability page, lineage records in artifacts, and richer source-groundwork summaries now exist | Full source-origin inference and spread-path modeling are still future work | `traceability.html`, lineage readmodels, `src/siasa/readmodels/traceability*.py`, `src/siasa/gui/local_app.py` | `tests/unit/test_local_gui.py` and related unit tests | `P4-WP-003` current package | P1/P2 runtime honesty or breadth package |
| Source dependency / source-origin groundwork | Surface coupling candidates and explicitly show what is still missing for origin inference | Done | Traceability view now exposes dependency cluster candidates with timing-lag context plus source-origin inference grounded in artifact-window `observed_at` timestamps (first-observed source classification with explicit uncertainty states) | Information-epidemiology depth and cross-run propagation modeling can still extend later, but the stakeholder-groundwork objective (coupling candidates + explicit origin inference transparency) is operationally closed | `traceability.html`, `traceability_lineage.json`, `src/siasa/gui/local_app.py`, `src/siasa/traceability/lineage.py`, `src/siasa/runs/orchestrator.py` | `tests/unit/test_local_gui.py`, `tests/unit/test_run_orchestrator.py`, full suite pass | P1 traceability origin-inference + lag-signals package | Optional later epidemiology-depth package |
| Reports / exports | Generate and inspect governed output artifacts | Done | Export/report view exists, exported files are linked and copied, evidence summary visible, and interactive report-scoping controls (type + report-id filtering with visible-row count) now support focused analyst export review | Future server-backed on-demand report generation can still extend beyond static-bundle scope, but stakeholder-facing inspect/scope behavior is now operational in the local GUI baseline | `reports.html`, export files under generated GUI output, `src/siasa/gui/local_app.py` | `tests/unit/test_local_gui.py`, full-suite pass, representative live probe + GUI evidence (`build/local_gui/_rep_report_scope_1`) | `P3 report/export scoping UX` current package | Optional later server-side generation workflow package |
| Analyst annotation visibility | See annotation context in country/domain/global views | Done | Annotation page, contextual rendering on country/domain pages, linked annotation details | Future review/admin workflows may still extend this | `annotations.html`, country/domain annotation sections | `tests/unit/test_local_gui.py` | Earlier annotation visibility package | None immediately required |
| Analyst annotation create/edit workflow | Operationally create/edit/filter/history/export annotations in GUI | Done | Browser-local create/edit/filter/history workflow, contextual quick links from country/domain pages, JSON export, prefilled workflow entry | Future governed server-side persistence/review flow may still come later | `annotations.html`, quick links from `countries/*.html` and `domains/*.html`, `src/siasa/gui/local_app.py` | `tests/unit/test_local_gui.py`, browser verification, full suite pass | `884659b [verified] feat: add local annotation workflow` | Optional later review/admin workflow |
| Historical / baseline comparison overlays | Explain status/trend deltas against baseline and history in an analyst-friendly way | Done | Trend rows now include historical comparison summaries and domain detail pages now include structured comparison-vs-baseline plus historical-window context | Broader country-level explanation overlays can still be extended later, but the targeted trend/domain gap is closed for the current GUI baseline | Trend/domain GUI pages in `src/siasa/gui/local_app.py`, generated `trends.html` and `domains/*.html` | `tests/unit/test_local_gui.py`, browser verification, full suite pass | `P4-WP-002` current package | `P4-WP-003` |
| Real source access | Productive use of real external sources | Done | Source-access assessment and governed live runtime pilot exist; real adapter baselines exist for selected sources; governed named pilot sets now cover the stable 4-country representative subset, the broader 6-country `core-focus-initial` subset (`UKR,RUS,CHN,TWN,ISR,POL`), the 7-country `core-focus-expanded` subset (`UKR,RUS,CHN,TWN,ISR,IND,POL`), the 9-country `core-focus-broader` subset (`UKR,RUS,CHN,TWN,IRN,ISR,TUR,IND,POL`), and the new 11-country `core-focus-complete` subset (`UKR,RUS,CHN,TWN,IRN,ISR,TUR,IND,PAK,GEO,POL`) | Full production-grade breadth and stable access across intended source classes remains incomplete, but the current governed core-focus set now also has a successful `release_verdict=ready` probe with `PAK` and `GEO` included; the next breadth step is no longer missing P1 core-focus countries but deciding whether to expand into broader MVP/P2 scope or deepen validation evidence | Current roadmap status in `docs/plans/siasa-stakeholder-fulfillment-roadmap.md`, live runtime code under `src/siasa/runs/live_runtime.py`, and probe artifact bundles `build/run_artifacts/_core_focus_expanded_probe_4`, `build/run_artifacts/_core_focus_broader_probe_1`, and `build/run_artifacts/_core_focus_complete_probe_1` | Repo tests, full-suite pass, and live probe evidence for `core-focus-expanded` / `core-focus-broader` / `core-focus-complete` | P0 readiness and later breadth packages advanced | Continue governed breadth expansion into broader MVP scope or deepen validation/reference cases |
| Representative multi-country runtime breadth | More than one governed country materially supported in latest outputs | Done | Multi-country runtime/artifact path works; representative live probes show the stable 4-country governed subset, the runtime also supports `core-focus-initial`, `core-focus-expanded`, `core-focus-broader`, and the new `core-focus-complete` subset with added `PAK` and `GEO`; TWN remains explicitly scoped to A/B while the added `RUS`, `CHN`, `IND`, `IRN`, `TUR`, `PAK`, and `GEO` countries run with A/B/D | Full-domain breadth beyond these subsets remains a later expansion topic, but the 11-country `core-focus-complete` subset now has a successful ready-state probe and closes the currently obvious core-focus breadth gap | Runtime code under `src/siasa/runs/live_runtime.py`, probe artifacts under `build/run_artifacts/_resil_probe_1` / `_resil_probe_2` / `build/run_artifacts/_core_focus_expanded_probe_4` / `build/run_artifacts/_core_focus_broader_probe_1` / `build/run_artifacts/_core_focus_complete_probe_1`, and readmodels such as `_core_focus_complete_probe_1/readmodels/world_map.json` and `_core_focus_complete_probe_1/readmodels/readiness.json` | Full suite pass, runtime-related tests, and successful `RUN-LIVE-CORE-EXP-RETRY-004` / `RUN-LIVE-CORE-BROAD-001` / `RUN-LIVE-CORE-COMPLETE-001` live probe evidence | Current P2 breadth extension package | Next breadth slice to broader MVP/P2 countries or validation/reference-case depth |
| Role-based UI behavior | Functions change by user role / permission model | Done | Local GUI now supports explicit role profiles (`viewer`, `analyst`, `admin`) with role-gated navigation and page generation; viewer mode intentionally suppresses ops/annotation surfaces while analyst/admin retain them | Server-side authn/authz and persisted identity/permission governance are still later architecture work | `src/siasa/gui/local_app.py`, `build/local_gui/_rep_role_1_analyst`, `build/local_gui/_rep_role_1_viewer` | `tests/unit/test_local_gui.py` role tests, full suite pass, representative live probe + dual-role GUI generation | `P3 role-based GUI behavior` current package | Later identity/governed permission model package |
| Rich analyst cockpit beyond static-view baseline | Dashboard-like exploratory product experience | Done | Current GUI is materially richer than before: controls, comparisons, overlays, annotation workflow, readiness, traceability, and now analyst-cockpit view-state persistence/sharing controls (save/restore/copy-link with URL-hash restore) for reproducible exploratory slices | A future server-backed SPA can still extend architecture, but the stakeholder-facing exploratory cockpit behavior is now operationally closed in the governed local GUI baseline | `src/siasa/gui/local_app.py`, generated `build/local_gui/*.html` | GUI tests and browser verifications (`tests/unit/test_local_gui.py`, full-suite pass) | P1 analyst cockpit state-persistence closure package | Optional later architecture uplift (server-backed SPA) |

---

## 3. Current work-package steering view

| Work package | Purpose | Current state | Evidence of closure / current truth | Capability impact |
| --- | --- | --- | --- | --- |
| P2 governed live runtime source expansion: UCDP integration | Add UCDP GED to the governed live runtime so the serial live stack exposes an additional real conflict source, then re-align adapter-order-sensitive tests and source-depth expectations | Done | Updated `src/siasa/runs/live_runtime.py` to import and append `UCDPAdapter` when supported-country coverage exists; updated `tests/unit/test_governed_live_runtime.py` for the new adapter ordering, supported-country expectations, and source-depth/source-coverage assertions; validation evidence: focused `tests/unit/test_governed_live_runtime.py -q` passed (`11 passed`), followed by full repository regression `tests -q` passed (`454 passed`) | Expands live governed source breadth in a traceable way and keeps the runtime/test contract aligned as real source coverage grows |
| P1 analyst interpretation depth: briefing synthesis across readiness, validation, traceability, and operability | Turn the readiness-facing analyst briefing into a more decision-oriented synthesis that surfaces traceability and operability risk alongside release, country-gap, validation, and stale-priority signals | Done | Updated `src/siasa/gui/local_app.py` so `Analyst Briefing — What matters now?` now pulls in traceability integrity and operability-cluster signals, adds a primary-focus summary line, and exposes richer counters/target-page guidance; updated `tests/unit/test_local_gui.py` for the new traceability/operability prioritization path and readiness-page rendering; validation evidence: focused `tests/unit/test_local_gui.py -q` passed (`18 passed`), followed by full repository regression `tests -q` passed (`454 passed`) | Improves analyst actionability by consolidating the highest-priority readiness, validation, traceability, and operability signals into one ordered decision view |
| P2 planning-governance hygiene: capability-matrix historical-gap section re-sync | Remove contradictory stale gap claims in section 5 of the capability matrix and align the historical note with current AP-F01..AP-F27 closure state + next-step framing tracks | Done | Updated `docs/plans/siasa-project-lead-capability-matrix.md` section 5 from obsolete 2026-05-30 open-gap table to a synchronized historical-marker section that explicitly states AP-F01..AP-F27 closure and current expansion tracks; validation evidence: targeted `tests/unit/test_readmodels_functional_fulfillment.py` + focused regressions `tests/unit/test_readmodels_release_evidence.py` and `tests/unit/test_traceability_consistency.py` passed; fulfillment remains `100.0%` (`Done=16/16`) | Prevents project-lead steering confusion caused by stale contradictory narrative and keeps governance document causally aligned with current repository evidence |
| P1 validation interpretation extension: replay-attention shareable filter-link hash state (card workflow) | Make card-based replay-attention slices reproducible/shareable by persisting filter state to URL hash and providing one-click copy-link handoff in validation view | Done | Updated `src/siasa/gui/local_app.py`: added `replay-attention-copy-link` + `replay-attention-link-status`; introduced `getReplayAttentionState`, `serializeReplayAttentionState`, `persistReplayAttentionStateToHash`, `applyReplayAttentionStateFromHash`, and `copyReplayAttentionFilterLink`; hash now stores deterministic `ra_level`, `ra_owner`, `ra_reason`, `ra_verdict`, `ra_tier`, `ra_text`, restores on page load, and clears when returning to default; updated `tests/unit/test_local_gui.py` assertions for new IDs/functions/hash keys; validation evidence: targeted `tests/unit/test_local_gui.py` (`17 passed`), focused regressions `tests/unit/test_run_artifacts.py` (`19 passed`) + `tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py` (`1 passed`); fulfillment probe remains `100.0%` (`Done=16/16`) | Restores deterministic async handoff of exact replay-attention triage slices in the current card-based UX without manual filter reconstruction |
| P1 validation interpretation extension: replay-attention verdict/tier scoping + visible verdict mix | Further sharpen card-based replay-attention triage by adding deterministic verdict/evidence-tier filters and a live verdict composition signal for the currently visible slice | Done | Updated `src/siasa/gui/local_app.py`: validation controls now include `replay-attention-verdict-filter` + `replay-attention-tier-filter`; JS filter chain extended (`applyReplayAttentionFilters`) with `reviewVerdict`/`replayTier` predicates, active-state now tracks verdict+tier, reset flow clears both controls, and visible-slice verdict composition is rendered in `replay-attention-visible-verdict-breakdown` via deterministic sorted `verdictCounts`; updated `tests/unit/test_local_gui.py` with assertions for new IDs and hooks; validation evidence: targeted `tests/unit/test_local_gui.py` (`17 passed`), focused regressions `tests/unit/test_run_artifacts.py` (`19 passed`) + `tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py` (`1 passed`); fulfillment probe remains `100.0%` (`Done=16/16`) | Improves triage precision and screenshot/handoff clarity by making visible mismatch/weak-evidence composition explicit and reproducible in-card workflow |
| P1 validation interpretation extension: replay-attention interactive card filters + active-state summary | Restore high-actionability triage on replay-attention watchlists by reintroducing deterministic client-side filters (level/owner/reason/free-text), visible-count feedback, and explicit active-slice summary while keeping card-based UX | Done | Updated `src/siasa/gui/local_app.py`: replay-attention cards now carry stable filter datasets (`replay-attention-card`, `data-attention-*`, `data-card-search-text`) and validation view now renders controls (`replay-attention-level-filter`, `replay-attention-owner-filter`, `replay-attention-reason-filter`, `replay-attention-text-filter`, `replay-attention-reset`) with JS hooks `applyReplayAttentionFilters`, `renderReplayAttentionActiveState`, `resetReplayAttentionFilters`; updated `tests/unit/test_local_gui.py` assertions for new IDs/hooks; validation evidence: targeted `tests/unit/test_local_gui.py` (`17 passed`), focused regressions `tests/unit/test_run_artifacts.py` (`19 passed`) + `tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py` (`1 passed`); fulfillment probe remains `100.0%` (`Done=16/16`) | Re-establishes reproducible replay-attention slice scoping and handoff clarity (what is currently visible and why) without leaving analysts in manual card scanning mode |
| AP-F05 persistierter `latest` Run-Zyklus + Verifikation | Stabilen Standard-Workflow liefern, der `build/run_artifacts/latest/` reproduzierbar befuellt (inkl. GUI) und mit einem expliziten Verifikationsskript pruefbar macht; plus klare Windows/PyCharm-Doku | Done | Added `src/siasa/runs/latest_bundle_verification.py` + CLI wrapper `scripts/verify_latest_bundle.py`; README um Linux + Windows/PyCharm Standard-Commands fuer `scripts/build_operational_latest_bundle.py` und Verifikation erweitert; neue Tests `tests/unit/test_latest_bundle_verification.py` (`3 passed`), plus Regression auf `tests/unit/test_operational_latest.py`, `tests/unit/test_validation_replay_depth_probe_readmodels.py`, `tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py`, `tests/unit/test_readmodels_release_failure_drill.py` (`13 passed`); full suite green (`305 passed`) | Schliesst die Luecke zwischen ad-hoc Live-Run und governed operatorischem `latest`-Zyklus mit explizitem Pass/Fail-Check und reproduzierbarer Team-Durchfuehrung (Linux + Windows/PyCharm) |
| AP-N05 lightweight data persistence (SQLite run history) | Persist run metadata + source execution outcomes for every operational latest build and expose deterministic recent-run/source-result queries | Done | Added `src/siasa/data/storage.py` (schema init, upsert run entries, source-result persistence, recent-run/source query APIs), wired persistence into `src/siasa/runs/operational_latest.py` (default DB `build/run_history/latest_runs.sqlite`) and CLI `scripts/build_operational_latest_bundle.py --history-db`; added inspection CLI `scripts/query_run_history.py`; README updated with Linux + Windows/PyCharm history commands; tests added/updated: `tests/unit/test_storage_run_history.py`, `tests/unit/test_operational_latest.py` | Closes AP-N05 by making `latest` runs auditable over time (not only single-snapshot artifacts), enabling AP-N06 scheduler and later trend/backtesting layers to consume deterministic run-history evidence |
| AP-F06 SQLite run-history domain scores and time-series queries | Persist per-country per-domain scores (status, score, data_sufficiency) for each run and expose time-series and latest-score query APIs for downstream trend/backtesting consumption | Done | Extended `src/siasa/data/storage.py` with `country_domain_scores` table, `persist_country_domain_scores()`, `load_country_domain_time_series()`, `load_latest_country_scores()`; extended `src/siasa/data/__init__.py` exports; extended `scripts/query_run_history.py` with `--country` and `--domain` flags; README updated with new query commands; tests: `tests/unit/test_storage_run_history.py` (4 new: persist+query, time-series ordering, latest-per-domain, upsert); targeted 7 passed + focused regression 6 passed | Closes the domain-score persistence gap so trend analysis, backtesting, and historical comparisons can consume governed per-country per-domain score history |
| AP-F07 daily run scheduler with failure alerting | Automated daily runs via lightweight time.sleep scheduler with structured logging, failure detection, log-file and optional webhook alerting, and configurable interval/pilot-set | Done | Added `src/siasa/runs/scheduler.py` with `execute_scheduled_run()`, `run_scheduler_loop()`, `send_alert()`, CLI `python -m siasa.runs.scheduler`; supports `--interval-hours`, `--alert-webhook`, `--alert-log-file`, `--log-file`; added `tests/unit/test_scheduler.py` (6 tests: success, failure, n-iteration loop, alert-on-failure, alert-to-log, run-id format); README updated with Linux + Windows/PyCharm scheduler commands; targeted 6 passed + focused regression 10 passed | Closes AP-F07 by making SIASA operationally self-running with governed failure detection and alerting, removing the need for manual ad-hoc run execution |
| AP-F08 health monitoring (liveness, staleness, source-availability, status-file) | Deterministic health checks for operational SIASA deployments covering liveness, artifact presence, staleness, run-history health, and source-availability probes with machine-readable status-file output | Done | Added `src/siasa/runs/health_monitor.py` with `check_liveness()`, `check_artifacts_exist()`, `check_artifacts_staleness()`, `check_run_history_health()`, `check_source_availability()`, `run_health_checks()`, `write_health_status()`, CLI `python -m siasa.runs.health_monitor`; supports `--max-staleness-hours`, `--skip-source-probe`, `--output`; added `tests/unit/test_health_monitor.py` (13 tests); README updated with health-check commands; targeted 13 passed + focused regression 13 passed | Closes AP-F08 by making operational health observable and machine-queryable, enabling dashboard/alerting integration and reducing silent-failure risk |
| AP-F09 historical data layer with configurable retention | Timestamped per-source per-country raw/normalized record storage in SQLite with configurable per-domain/per-source retention horizons and automated pruning for downstream trend analysis and backtesting | Done | Added `src/siasa/data/historical.py` with `persist_historical_records()`, `load_historical_records()`, `set_retention_horizon()`, `get_retention_horizon()`, `prune_expired_records()`; tables: `historical_records` + `retention_config`; added `tests/unit/test_historical_data.py` (5 tests: persist+query, filters, retention config, default pruning, custom-retention pruning); targeted 5 passed + focused regression 26 passed | Closes AP-F09 by providing the governed historical data foundation needed for AP-F10 backtesting and AP-F11 configurable freshness windows |
| AP-F11 configurable freshness windows per domain and source | YAML-governed per-domain and per-source freshness thresholds with deterministic resolution order (source override > domain default > global fallback) replacing hardcoded 168h | Done | Added `vmodel/project/freshness_config.yaml` with governed domain defaults (A/B/D/E=168h, C=336h) and source-override section; added `src/siasa/scoring/freshness_config.py` with `resolve_freshness_threshold()`, `get_all_freshness_thresholds()`, cache management; added `tests/unit/test_freshness_config.py` (7 tests: domain default, source override, global fallback, missing file, priority order, real-config smoke); targeted 7 passed + focused regression 12 passed | Closes AP-F11 by making freshness thresholds governed and configurable without code changes, enabling per-source tuning for sources with different update frequencies |
| AP-F12 UCDP conflict data adapter | UCDP GED (Georeferenced Event Dataset) adapter for curated armed conflict data — complements GDELT Events with academic-quality conflict event counts, fatality estimates, and state-based violence signals; graceful degradation without API token | Done | Added `src/siasa/adapters/ucdp.py` with `UCDPAdapter` (SourceAdapter subclass, domain=B, source_id=SRC-UCDP-GED): paginated GED API fetch with retry/backoff, ISO-3 to UCDP numeric country ID mapping (21 pilot countries), per-country aggregation of `armed_conflict_events`, `battle_deaths_best`, `state_based_events`; configurable API token via `UCDP_API_TOKEN` env var or explicit param with clear diagnostics on missing token; updated `src/siasa/adapters/__init__.py` exports; added `tests/unit/test_adapter_ucdp.py` (13 tests: no-token graceful fail, env-var token pickup, single-page aggregation, country filtering, multi-page pagination, freshness computation, unknown country skip, retry success, retry exhaustion, source-id/domain check, pilot mapping completeness, mapping uniqueness); targeted 13 passed + focused regression 39 passed | Closes AP-F12 by adding a curated academic conflict data source to Domain B, broadening source class coverage beyond GDELT/GDACS and addressing StR-205..214 (ACLED/UCDP alternative) |
| AP-F13 UNHCR displacement data adapter (Domain C) | UNHCR Population Statistics adapter for forced displacement data — refugee populations, asylum seekers, IDPs, and total displacement per country of origin; no API key required; complements GDACS disaster alerts with structured displacement indicators | Done | Added `src/siasa/adapters/unhcr.py` with `UNHCRPopulationAdapter` (SourceAdapter subclass, domain=C, source_id=SRC-UNHCR-POP): paginated UNHCR Population API fetch with retry/backoff, per-country/year aggregation using most-recent-year selection, signals `refugee_population`, `asylum_seeker_population`, `idp_population`, `displacement_total`; handles UNHCR API quirks (dash values, string ints); updated `src/siasa/adapters/__init__.py` exports; added `tests/unit/test_adapter_unhcr.py` (17 tests: single-country aggregation, most-recent-year selection, country filtering, no-filter inclusion, multi-page pagination, freshness computation, zero-value exclusion, dash-value handling, retry success, retry exhaustion, source-id/domain check, plus 5 _safe_int helper tests); targeted 17 passed + focused regression 53 passed | Closes AP-F13 by adding a public displacement data source to Domain C, broadening source class coverage with UNHCR refugee/IDP/asylum data alongside GDACS disaster alerts |
| AP-F14 CISA KEV cyber-threat adapter (Domain E) | CISA Known Exploited Vulnerabilities catalog adapter for global cyber-threat indicators — recent KEV additions, ransomware-linked KEVs, overdue remediation count, and total catalog size; fully public, no API key; broadcast as global context to all target countries with explicit quality_flag=cisa_kev_global | Done | Added `src/siasa/adapters/cisa_kev.py` with `CISAKEVAdapter` (SourceAdapter subclass, domain=E, source_id=SRC-CISA-KEV): single-fetch JSON catalog with retry/backoff, signals `cyber_kev_recent_count`, `cyber_kev_ransomware_recent`, `cyber_kev_overdue_count`, `cyber_kev_total`; configurable `recent_days` window (default 30); default pilot set broadcast when no country_ids; updated `src/siasa/adapters/__init__.py` exports; added `tests/unit/test_adapter_cisa_kev.py` (11 tests: basic fetch, recent-count windowing, ransomware counting, overdue counting, multi-country broadcast, default pilot set, empty catalog, freshness=0, retry success, retry exhaustion, source-id/domain check); targeted 11 passed + focused regression 64 passed | Closes AP-F14 by adding a structured cyber-threat intelligence source to Domain E, complementing GDELT Doc cyber-mention data with curated exploit-threat indicators |
| AP-F15 ReliefWeb humanitarian reports adapter (Domain C) | ReliefWeb Reports v2 API adapter for humanitarian situation report counts and source diversity per country; graceful degradation without pre-approved appname (HTTP 403 since Nov 2025); complements GDACS and UNHCR with crisis reporting intensity | Done | Added `src/siasa/adapters/reliefweb.py` with `ReliefWebAdapter` (SourceAdapter subclass, domain=C, source_id=SRC-RELIEFWEB): per-country report queries with retry/backoff, signals `humanitarian_report_count`, `humanitarian_report_sources`; ISO-3 to ReliefWeb country-name mapping (21 pilot countries); configurable appname via `RELIEFWEB_APPNAME` env var with clear diagnostics on missing appname; per-country fault isolation (one country failure does not abort others); updated `src/siasa/adapters/__init__.py` exports; added `tests/unit/test_adapter_reliefweb.py` (12 tests: no-appname graceful fail, env-var appname pickup, single-country aggregation, multi-country, no-reports exclusion, freshness, unmapped country skip, per-country fault isolation, retry success, source-id/domain check, pilot mapping completeness, mapping validity); targeted 12 passed + focused regression 57 passed | Closes AP-F15 by making ReliefWeb integration-ready with governed appname configuration; adapter is fully functional pending RELIEFWEB_APPNAME registration |
| AP-F10 automated backtesting pipeline | Pipeline that compares current scoring results against historical baselines from the run-history DB, detects regressions (score drops > 10%), status changes, and generates markdown reports with pass/warning/fail verdicts | Done | Added `src/siasa/validation/backtesting.py` with `run_backtest()`, `generate_backtest_report()`, `_compare_scores()`, `_discover_available_scores()`; dataclasses `BacktestScoreSnapshot`, `BacktestComparison`, `BacktestResult`; configurable country/domain filters and baseline offset; regression rate thresholds (>10% warning, >30% fail); added `tests/unit/test_backtesting.py` (13 tests: basic pass, regression detection, status change tracking, multi-country/domain, country filter, domain filter, insufficient history, empty DB, high regression rate fail, report summary, report regression listing, stable score comparison, large score drop comparison); targeted 13 passed + focused regression 22 passed | Closes AP-F10 by providing automated regression detection against historical scoring baselines, enabling CI-level backtesting gates and analyst-facing trend validation |
| AP-F20 rule-based assessment engine | YAML-configurable rule catalog with condition/operator/value matching against domain and multi-domain scoring results; produces annotation drafts with severity, tags, and templated text when conditions match; supports eq/gt/lt/gte/lte/in/not_eq operators and enable/disable per rule | Done | Added `vmodel/project/assessment_rules.yaml` with 7 governed rules (D4 critical, D3 elevated, D0 data gap, D5 contradictory, high anomaly >0.8, S5/S6 critical multi-domain, S4 elevated multi-domain); added `src/siasa/scoring/rule_engine.py` with `load_assessment_rules()`, `evaluate_rules()`, `AssessmentRule`, `AssessmentContext`, `RuleEvaluationResult`, condition evaluator, and template renderer; added `tests/unit/test_rule_engine.py` (18 tests: YAML loading, real config smoke, duplicate rejection, D4 trigger, high anomaly trigger, multi-domain trigger, disabled rule skip, no-match empty, multiple rules same context, multi-country evaluation, plus 8 condition operator tests); targeted 18 passed + focused regression 38 passed | Closes AP-F20 by enabling automatic rule-driven assessments during runs, replacing purely manual annotation workflow with governed configurable rule catalog (StR-266..281) |
| AP-F21 automatic report generation | Daily snapshot reports (status distribution, country details, overall status) and anomaly reports (severity-sorted rule-triggered anomalies) with configurable YAML templates and markdown output | Done | Added `src/siasa/reporting/auto_reports.py` with `generate_daily_snapshot_report()`, `generate_anomaly_report()`, `load_report_config()`, dataclasses `DailySnapshotReport`, `AnomalyReport`, `ReportConfig`; daily snapshot computes per-country worst status, average score, overall status (critical/elevated/contradictory/data_gaps/normal); anomaly report aggregates rule evaluation results sorted by severity; both generate structured markdown; added `tests/unit/test_auto_reports.py` (15 tests: basic snapshot generation, markdown structure, country summaries, normal/data_gaps/empty status, report ID format, anomaly generation, anomaly markdown, severity sorting, empty anomalies, report ID format, config loading, default config, config country limit); targeted 15 passed + focused regression 46 passed | Closes AP-F21 by enabling automatic report generation from run results, eliminating manual report creation and addressing StR-301..310 (automatische Reports) |
| AP-F16 interactive world map (zoom/pan/overlay) | SVG map enhanced with mouse-wheel zoom (0.5x-8x), drag-to-pan, cursor-following tooltip (status+anomaly+freshness), and three-mode overlay switcher (Status/Anomaly Score/Freshness coloring) with Reset Zoom button; all client-side JS in generated HTML | Done | Extended `src/siasa/gui/local_app.py` `_render_world_map_visualization()`: added `map-zoom-container` wrapper with overflow:hidden, zoom/pan JS (wheel+drag with transform), floating `map-tooltip` with rich hover data, `map-overlay-controls` with 3 overlay buttons + reset; added `data-anomaly-score` and `data-freshness-hours` data attributes to country path elements; overlay switcher recolors countries by status (original), anomaly score (6-bucket green-to-red), or freshness (fresh/stale/very_stale); updated `tests/unit/test_local_gui.py` with 11 new assertions (zoom-container, overlay-controls, overlay-btn, tooltip, zoom-reset, data-overlay variants, applyOverlay, applyTransform, data-anomaly-score); targeted 17 passed + all assertions green | Closes AP-F16 by making the world map interactive with zoom/pan navigation and switchable score overlays (StR-101..110) |
| AP-F17 enhanced trend charts (zoom/pan/range/multi-series) | Per-chart SVG zoom/pan via wheel+drag, time-range selection buttons (6M/1Y/All) with data-point opacity fading, multi-series toggle checkboxes, and data-idx attributes on chart circles for range-aware highlighting | Done | Added `_render_enhanced_trend_controls_js()` to `src/siasa/gui/local_app.py`: per-chart zoom/pan on `.trend-chart-block svg`, range buttons (`.trend-range-btn` with `data-range`), multi-series toggle (`.trend-series-toggle`); added `data-idx` to chart circle elements; added `trend-range-controls` div with 6M/1Y/All buttons per trend row; wired enhanced JS into `_render_trends()` output; updated `tests/unit/test_local_gui.py` with 7 new assertions (trend-range-btn, data-range variants, trend-range-controls, data-idx); targeted 17 passed | Closes AP-F17 by enabling interactive chart exploration with zoom, pan, and time-range scoping (StR-111..117) |
| AP-F18 multi-window parallel comparison | Side-by-side dual-panel country comparison on the comparison page — two dropdown selectors for left/right country, grid-based panels that dynamically render status, domains, coverage, confidence, and drivers; JS-driven without page reload | Done | Extended `_render_comparison()` in `src/siasa/gui/local_app.py`: added `multi-window-controls` div with `mw-country-left` and `mw-country-right` select elements populated from all available countries; `multi-window-panels` CSS-grid (1fr 1fr) with `mw-panel-left` and `mw-panel-right`; `mwData` JS object pre-populated with per-country status/domains/coverage/confidence/drivers; `renderMWPanel()` JS function renders rich HTML on selection change; updated `tests/unit/test_local_gui.py` with 7 new assertions (multi-window-controls, mw-country-left/right, mw-panel-left/right, multi-window-panels, renderMWPanel); targeted 17 passed | Closes AP-F18 by enabling dual-country parallel comparison views (StR-118..128) |
| AP-F19 dynamic role switching | Client-side role switcher (Analyst/Admin/Viewer) in every page nav bar — without regenerating the GUI; viewer mode hides ops/annotation/readiness nav links; `data-role-min` attribute support for fine-grained element gating; role state persisted on `document.body.dataset.activeRole` | Done | Extended `_page()` in `src/siasa/gui/local_app.py`: added `role-switcher` div with `role-select` dropdown (analyst/admin/viewer) in the nav bar; `applyRole()` JS function hides/shows `[data-role-min]` elements and ops nav links based on role hierarchy (viewer < analyst < admin); wired into every generated page; updated `tests/unit/test_local_gui.py` with 7 new assertions (role-switcher, role-select, applyRole, value variants, activeRole); targeted 17 passed | Closes AP-F19 by enabling dynamic role switching without GUI regeneration (StR-081..088) |
| AP-F22 cross-domain evidence fusion | Explicit contradiction detection between domain pairs (A/D, A/B, B/D, C/B), data-quality evidence weighting, weighted anomaly-score fusion, and confidence scoring with contradiction penalty | Done | Added `src/siasa/scoring/cross_domain_fusion.py` with `detect_cross_domain_contradictions()`, `compute_evidence_weights()`, `fuse_domain_evidence()`; dataclasses `ContradictionResult`, `EvidenceWeight`, `FusionResult`; severity-gap >= 2 triggers contradiction; weights normalized by sufficiency (1.0/0.6/0.2); confidence reduced by max-gap penalty; added `tests/unit/test_cross_domain_fusion.py` (16 tests); targeted 16 passed | Closes AP-F22 (StR-045..050) |
| AP-F23 dependency graph and cluster detection | Source dependency graph with connected-component clustering, cohesion metrics, and per-source influence scores (in/out degree) | Done | Added `src/siasa/analysis/dependency_graph.py` with `DependencyEdge`, `DependencyCluster`, `InfluenceScore`, `build_dependency_graph()`; added `tests/unit/test_dependency_graph.py` (5 tests); targeted 5 passed | Closes AP-F23 (StR-029..034) |
| AP-F24 provenance graph | Full data lineage tracking from source to multi-domain with BFS depth, root/leaf detection, and cross-run provenance comparison | Done | Added `src/siasa/analysis/provenance_graph.py` with `ProvenanceNode`, `ProvenanceEdge`, `ProvenanceChain`, `build_provenance_chain()`, `compare_provenance_runs()`; added `tests/unit/test_provenance_graph.py` (3 tests); targeted 3 passed | Closes AP-F24 (StR-035..039) |
| AP-F25 information epidemiology | Spread-path modeling across sources via timing-ordered observation sequences and amplification detection (factor > 1.5x threshold) | Done | Added `src/siasa/analysis/info_epidemiology.py` with `SpreadObservation`, `SpreadPath`, `AmplificationEvent`, `detect_spread_paths()`, `detect_amplification()`; added `tests/unit/test_info_epidemiology.py` (4 tests); targeted 4 passed | Closes AP-F25 (StR-040..044) |
| AP-F26 probabilistic scoring | Bayesian D-status classification with Gaussian likelihood, MAP estimate, posterior probabilities, 80% credible intervals, and status transition probabilities | Done | Added `src/siasa/scoring/probabilistic.py` with `BayesianStatusEstimate`, `compute_bayesian_status()`, `transition_probability()`; added `tests/unit/test_probabilistic_scoring.py` (5 tests); targeted 5 passed | Closes AP-F26 (StR-057..061) |
| AP-F27 uncertainty propagation | Source-to-multi-domain uncertainty propagation with per-stage noise injection (extraction 10%, scoring 15%, fusion 5%), 90% confidence intervals, dominant-stage identification, and propagation factor | Done | Added `src/siasa/scoring/uncertainty_propagation.py` with `UncertaintyLevel`, `UncertaintyBudget`, `propagate_uncertainty()`; added `tests/unit/test_uncertainty_propagation.py` (5 tests); targeted 5 passed | Closes AP-F27 (StR-051..056) |
| P1 planning-governance extension: functional stakeholder-fulfillment estimator | Add a reproducible estimator for functional stakeholder-fulfillment percentage from the capability matrix so each serial update can report a stable, auditable steering value | Done | Added `src/siasa/readmodels/functional_fulfillment.py` plus unit tests `tests/unit/test_readmodels_functional_fulfillment.py`; estimator parses section "Current project-lead capability matrix" and computes weighted progress with default weights `Done=1.0`, `Partial=0.6`, `Weak=0.3`, `Missing=0.0`, `Deferred by intent=0.2`; targeted test pass + full suite pass; representative live probe `RUN-LIVE-REP-FULFILL-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), GUI bundle `build/local_gui/_rep_fulfill_probe_1` generated, readiness `known_gaps=[]`, `release_verdict=ready`; current computed estimate: `100.0%` (`Done=16`, `Partial=0`, `Weak=0`, `N=16`) | Gives project-lead steering a consistent quantitative progress signal aligned with the existing capability matrix semantics |
| AP-01 stakeholder requirement quality normalization | Normalize and bound all active stakeholder requirements before downstream traceability/verification work, while preserving the raw workbook-derived baseline | Done | Added `vmodel/verification/stakeholder_requirement_quality_criteria.yaml`, `src/siasa/readmodels/stakeholder_requirement_quality.py`, `scripts/build_stakeholder_requirement_quality_report.py`, and unit tests in `tests/unit/test_readmodels_stakeholder_requirement_quality.py`; generated report shows `active_requirement_count=668`, `quality_closed_count=668`, `quality_gap_count=0`, `canonical_acceptance_source_count=375`, `ap01_policy_acceptance_source_count=293`, `source_priority_tbd_count=350`, and all AP-01 stop criteria pass; AP-01 boundary explicitly states quality closure does not claim product closure | Converts the imported StR baseline into an auditable quality-normalized steering baseline for AP-02/AP-03 without silently rewriting stakeholder source input |
| AP-02 stakeholder-to-system traceability closure | Ensure every AP-01 quality-normalized active stakeholder requirement has explicit system-level coverage and synchronized traceability links before software/test closure | Done | Extended `vmodel/requirements/system_requirements.yaml` from 30 to 50 SyR by adding targeted governance/system-behavior requirements `SyR-031..050` only for previously uncovered StR clusters; updated `vmodel/verification/test_specifications.yaml` with matching `TC-SyR-031-001..TC-SyR-050-001`; synchronized `vmodel/traceability/trace_links.yaml`; added `src/siasa/readmodels/stakeholder_system_traceability.py`, `scripts/build_stakeholder_system_traceability_report.py`, and tests in `tests/unit/test_readmodels_stakeholder_system_traceability.py`; generated report shows `active_stakeholder_requirement_count=668`, `covered_by_system_requirement_count=668`, `covered_by_traceability_count=668`, `missing_system_requirement_mapping_count=0`, `missing_traceability_mapping_count=0`, and `syr_trace_mismatch_count=0` | Closes the full StR->SyR layer for all active stakeholder requirements and creates a hard AP-02 gate before AP-03 software/test closure |
| AP-03 stakeholder verification route coverage | Ensure every active stakeholder requirement has an explicit governed verification route through SyR/TC, with software-route SyR additionally covered by SwR/TC and governance-route SyR explicitly verified at system level | Done | Added `vmodel/verification/stakeholder_verification_coverage_policy.yaml` defining `software_route` (`SyR-001..030`) and `governance_system_route` (`SyR-031..050`); added `src/siasa/readmodels/stakeholder_verification_coverage.py`, `scripts/build_stakeholder_verification_coverage_report.py`, and tests in `tests/unit/test_readmodels_stakeholder_verification_coverage.py`; generated report shows `active_stakeholder_requirement_count=668`, `stakeholder_verification_covered_count=668`, `missing_stakeholder_verification_count=0`, `software_route_system_requirement_count=30`, `governance_system_route_requirement_count=20`, `accepted_software_requirement_count=51`, and `software_requirements_without_tc_count=0` | Closes AP-03 verification-specification coverage for every stakeholder requirement while preserving the boundary that flow-level E2E execution remains AP-04 |
| AP-04 stakeholder E2E flow coverage gate | Define and gate prioritized stakeholder-facing role flows so requirement closure is demonstrably connected to real user journeys, happy paths, failure paths, and evidence tests/scripts | Done | Added `vmodel/verification/stakeholder_e2e_flows.yaml` with six prioritized flows (`project_lead`, `analyst`, `governance_admin`, `viewer`) covering readiness go/no-go, country/domain drilldown, validation replay triage, source/traceability governance, report/evidence governance, and role-boundary behavior; added `src/siasa/readmodels/stakeholder_e2e_flow_coverage.py`, `scripts/build_stakeholder_e2e_flow_coverage_report.py`, and tests in `tests/unit/test_readmodels_stakeholder_e2e_flow_coverage.py`; generated report shows `flow_count=6`, `covered_flow_count=6`, `flow_gap_count=0`, `covered_role_count=4/4`, and `missing_evidence_ref_count=0` | Moves from requirement/verification-spec closure into role-based end-to-end stakeholder journey evidence while keeping deeper browser automation as optional future hardening |
| AP-05 release gate hardening against stakeholder E2E flow gaps | Make AP-04 E2E flow coverage release-relevant by integrating the flow gate into the Release Readiness Index, CI, evidence pack, and failure-drill scenarios | Done | Extended `src/siasa/readmodels/release_evidence.py` so repository release assessment now includes `stakeholder_e2e_flow_coverage`, adds readiness gate `stakeholder_e2e_flows_covered`, and reports AP-04 flow coverage in markdown; added `scripts/ci_stakeholder_e2e_flow_coverage_check.py` and wired it into `.github/workflows/vmodel-ci.yml`; extended failure drill with `stakeholder_e2e_flow_gap_injected` and scenario artifact output; updated `docs/verification/release-go-no-go-runbook.md`; targeted release/e2e tests and CI scripts pass | Prevents release evidence from staying green when stakeholder-facing journey coverage regresses; AP-04 is now an enforced release-readiness condition, not only a standalone analysis report |
| AP-06 readiness UI governance visibility extension | Surface release-readiness governance directly in readiness UX (not only CI/JSON) by rendering the Release Readiness Index (gates, pass/fail, details) and AP-04/AP-05 E2E flow coverage stop-criteria in `readiness.html` with artifact-backed payload support | Done | Extended `src/siasa/gui/local_app.py` to load optional `readmodels/stakeholder_e2e_flow_coverage.json` + `readmodels/release_readiness_index.json`, render new readiness panels (`Release Readiness Index`, `Stakeholder E2E Flow Coverage (AP-04/AP-05)`), persist generated `stakeholder_e2e_flow_coverage.json` and `release_readiness_index.json`, and pass through artifact payload keys; updated `tests/unit/test_local_gui.py` for payload/load/file/rendering coverage including drill scenario `stakeholder_e2e_flow_gap_injected`; targeted GUI/readmodel tests pass and full suite passes (`284`) | Closes the operator visibility gap between machine governance gates and human readiness review, making AP-05 enforcement directly inspectable in shipped GUI outputs |
| AP-07 stakeholder E2E UI smoke governance gate | Add an executable GUI-level smoke gate for all six AP-04 role flows (project lead, analyst, governance admin, viewer) so CI enforces not only YAML/evidence refs but also rendered UX-path presence and role boundaries | Done | Added `src/siasa/readmodels/stakeholder_e2e_ui_smoke.py` with generated-bundle checks for all six AP-04 flow IDs, plus `scripts/ci_stakeholder_e2e_ui_smoke_check.py`; CI workflow now runs this gate after AP-04 coverage check; added `tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py` (`1 passed`) and updated `vmodel/verification/stakeholder_e2e_flows.yaml` evidence refs | Elevates AP-04 from static flow-definition coverage to executable rendered-flow governance in CI and reduces risk of silent UI path regressions |
| AP-08 release-readiness index extension for executable AP-04 UI smoke coverage | Make AP-07 release-relevant by integrating stakeholder E2E UI smoke closure into the Release Readiness Index and failure-drill governance, so go/no-go cannot stay green if rendered role-flow paths regress | Done | Extended `src/siasa/readmodels/release_evidence.py` with new readiness gate `stakeholder_e2e_ui_smoke_covered`, wired CI/tooling checks to include `scripts/ci_stakeholder_e2e_ui_smoke_check.py`, added AP-08 failure-drill scenario `stakeholder_e2e_ui_smoke_gap_injected`, and propagated assessment payload with `stakeholder_e2e_ui_smoke`; updated tests `tests/unit/test_readmodels_release_evidence.py` and `tests/unit/test_readmodels_release_failure_drill.py` to 8-gate expectations and AP-08 drill assertions | Closes the governance gap between AP-07 UI smoke existence and release decisioning by fail-closing go/no-go when executable AP-04 role-flow smoke no longer holds |
| AP-09 release-readiness stale-remediation actionability gate | Fail-close release readiness when stale-country risk exists but prioritized remediation actions are not explicitly actionable in governed artifacts/CI | Done | Extended `src/siasa/readmodels/release_evidence.py` with gate `stale_remediation_actionable` (requires actionable stale/remediation watchlists when stale countries exist), expanded workflow/tooling checks, and added failure-drill scenario `stale_remediation_gap_injected`; added `scripts/ci_stale_remediation_actionability_check.py`, wired step in `.github/workflows/vmodel-ci.yml`, updated `src/siasa/runs/artifacts.py` to persist `stakeholder_e2e_flow_coverage.json` + `stakeholder_e2e_ui_smoke.json` + `release_readiness_index.json`, and aligned tests `tests/unit/test_readmodels_release_evidence.py`, `tests/unit/test_readmodels_release_failure_drill.py`, `tests/unit/test_run_artifacts.py`, `tests/unit/test_local_gui.py`; serial closure validation: targeted `38 passed`, full suite `285 passed` | Prevents green go/no-go when stale coverage is known but not operationally actionable, improving freshness/remediation truthfulness in release governance |
| AP-10 UI smoke recursion guard and deterministic release-evidence execution | Remove recursive self-invocation between local GUI generation and AP-07 UI smoke reporting so release/readiness checks terminate deterministically and stay CI-usable | Done | `src/siasa/readmodels/stakeholder_e2e_ui_smoke.py` now calls local GUI subprocess with `--skip-stakeholder-e2e-ui-smoke-report`; `src/siasa/gui/local_app.py` adds this flag and injects a non-recursive placeholder view-model for smoke self-builds; corrected `build_local_mvp_site(...)` `report_catalog` default/type to dict to preserve existing GUI unit behavior; regression set passes (`tests/unit/test_readmodels_stakeholder_e2e_ui_smoke.py`, `tests/unit/test_readmodels_release_evidence.py`, `tests/unit/test_readmodels_release_failure_drill.py`, focused `tests/unit/test_local_gui.py` cases) and full suite passes (`285 passed`) | Restores fail-fast, reproducible release-governance execution path by eliminating hidden recursion hang and keeping GUI + release evidence gates operational |
| AP-11 stakeholder browser E2E acceptance gate | Move from structural UI-smoke presence checks to executable stakeholder journey acceptance checks (browser-verified role bundles + role boundaries + no-broken-link guarantees) for AP-04 flows | Done | Added `src/siasa/readmodels/stakeholder_browser_e2e_acceptance.py` and `tests/unit/test_readmodels_stakeholder_browser_e2e_acceptance.py`; added CI gate script `scripts/ci_stakeholder_browser_e2e_acceptance_check.py` and wired step in `.github/workflows/vmodel-ci.yml`; integrated into `src/siasa/readmodels/release_evidence.py` as new gate `stakeholder_browser_e2e_acceptance_covered` plus failure-drill scenario `stakeholder_browser_e2e_gap_injected`; persisted artifact `readmodels/stakeholder_browser_e2e_acceptance.json` in `src/siasa/runs/artifacts.py`; updated tests (`test_readmodels_release_evidence.py`, `test_readmodels_release_failure_drill.py`, `test_run_artifacts.py`); targeted set pass (`23 passed`) and full suite pass (`286 passed`) | Closes the AP-07→operability realism gap by fail-closing release governance when role-bundle execution or internal-link integrity regresses |
| AP-12 browser interaction-depth acceptance gate | Deepen AP-11 from bundle/link integrity to deterministic stakeholder click-path semantics across overview/readiness/coverage/country/domain/validation/reports transitions | Done | Added `src/siasa/readmodels/stakeholder_browser_interaction_depth.py` + `tests/unit/test_readmodels_stakeholder_browser_interaction_depth.py`; added CI gate `scripts/ci_stakeholder_browser_interaction_depth_check.py` and wired in `.github/workflows/vmodel-ci.yml`; integrated new release-readiness gate `stakeholder_browser_interaction_depth_covered` and failure-drill scenario `stakeholder_browser_interaction_depth_gap_injected` in `src/siasa/readmodels/release_evidence.py`; persisted `readmodels/stakeholder_browser_interaction_depth.json` via `src/siasa/runs/artifacts.py`; updated tests (`test_readmodels_release_evidence.py`, `test_readmodels_release_failure_drill.py`, `test_run_artifacts.py`); targeted pass (`23 passed`) and full suite pass (`287 passed`) | Closes interaction-depth operability risk by fail-closing release governance when deterministic click-path transition semantics regress |
| AP-13 browser interaction failure-mode resilience gate | Harden browser governance against negative-path regressions (role misrouting, missing drill-down/navigation targets, broken internal-link targets) that can pass happy-path checks | Done | Added `src/siasa/readmodels/stakeholder_browser_failure_resilience.py` + `tests/unit/test_readmodels_stakeholder_browser_failure_resilience.py`; added CI gate `scripts/ci_stakeholder_browser_failure_resilience_check.py` and wired in `.github/workflows/vmodel-ci.yml`; integrated release-readiness gate `stakeholder_browser_failure_resilience_covered` and failure-drill scenario `stakeholder_browser_failure_resilience_gap_injected` in `src/siasa/readmodels/release_evidence.py`; persisted `readmodels/stakeholder_browser_failure_resilience.json` via `src/siasa/runs/artifacts.py`; updated tests (`test_readmodels_release_evidence.py`, `test_readmodels_release_failure_drill.py`, `test_run_artifacts.py`); targeted pass and full suite pass (`288 passed`) | Closes deterministic negative-path detection risk by fail-closing release governance when navigation resilience or role-routing constraints regress |
| AP-14 release-drill enrichment: remediation hints + failure localization | Make release failure-drill outcomes immediately actionable by emitting per-scenario failed-gate localization and concrete remediation hints for AP-11..AP-13 browser governance and core readiness blockers | Done | Extended `src/siasa/readmodels/release_evidence.py` so `build_release_failure_drill_report(...)` now emits `failure_localization` (scenario->failed gate list with labels+hints) and validates non-empty localization for all injected scenarios via new check `failure_localization_nonempty_for_injected_scenarios`; updated `scripts/release_failure_drill_check.py` to print localization summary; expanded `tests/unit/test_readmodels_release_failure_drill.py` to assert localization coverage and key gate hint mapping; targeted pass (`3 passed`) and full suite pass (`288 passed`) | Closes operator-actionability gap by turning drill failures into directly triageable, remediation-ready outputs instead of raw pass/fail only |
| AP-14b browser gate diagnostics export (per-gate failure evidence slices) | Export deterministic per-gate diagnostics slices so each failing gate directly maps to affected injected scenarios, failure source, and remediation hint for faster triage/handoff | Done | Extended `src/siasa/readmodels/release_evidence.py` with `gate_diagnostics_export` (`gate_id -> failed_in_scenarios[]`) plus check `gate_diagnostics_export_nonempty_for_failed_gates`; enriched `scripts/release_failure_drill_check.py` summary and `scripts/build_release_failure_drill_pack.py` output with `failure_localization` + `gate_diagnostics_export`; failure-drill pack now writes per-gate JSON slices under `build/release_failure_drill/latest/gate_diagnostics/*.json`; expanded `tests/unit/test_readmodels_release_failure_drill.py`; targeted pass (`3 passed`) and full suite pass (`288 passed`) | Closes AP-14b triage/handoff latency risk by making gate-level failure evidence directly consumable as exported diagnostics artifacts |
| P2 planning-governance extension: stakeholder functional closure-gap plan (StR->SyR->SwR->TC) | Convert the identified 19 functional stakeholder traceability gaps into an executable closure backlog with concrete new SwR and TC proposals | Done | Added `docs/plans/siasa-stakeholder-functional-closure-gap-plan.md` with repo-evidenced gap baseline (`224 functional`, `205 umgesetzt`, `19 nicht_nachweisbar`), explicit affected StR IDs, and concrete proposed additions `SwR-046..051` plus `TC-SwR-046..051` candidate specs and serial implementation order | Creates a deterministic closure path from stakeholder intent to testable software-level evidence for the remaining functional traceability gaps |
| P2 planning-governance closure: encode functional stakeholder gap set as governed SwR/TC baseline | Convert the planned closure backlog into canonical V-model requirements/test artifacts by adding explicit software requirements and test specifications for the 19 functional StR gaps | Done | Extended `vmodel/requirements/software_requirements.yaml` with `SwR-046..051` and updated `software_requirement_count` to `51`; extended `vmodel/verification/test_specifications.yaml` with `TC-SwR-046-001`..`TC-SwR-051-001`; these additions explicitly cover Mission/Scope output contract, anti-deterministic and anti-naive-fusion guardrails, relative-baseline anomaly semantics, historical horizon governance, daily incremental snapshot-manifest contract, and layered data-state separation | Closes the missing formal traceability bridge for the previously non-nachweisbar functional stakeholder clusters and prepares executable closure in subsequent implementation/test packages |
| P2 planning-governance hardening: executable stakeholder-gap closure path gate | Add an automated integrity gate proving that each of the 19 previously non-traceably-closed functional stakeholder requirements now has an explicit stakeholder->system->software derivation path through `SwR-046..051` | Done | Added `test_functional_stakeholder_gap_cluster_has_explicit_stakeholder_to_software_closure_paths()` in `tests/unit/test_traceability_consistency.py`; test parses `vmodel/traceability/trace_links.yaml`, builds `SyR->StR` and `SwR->SyR` derivation maps, and asserts full coverage of `StR-001,002,004,007,024,025,135..142,226..230` via closure SwR set `SwR-046..051`; targeted traceability test pass (`20 passed`) and full suite pass (`272 passed`) | Converts the new formal SwR baseline into continuously validated stakeholder-closure evidence and prevents silent regression of the closure path |
| P2 planning-governance closure hardening: stakeholder functional closure readmodel artifact | Produce a machine-readable closure artifact in each run bundle that reports functional StR closure and explicitly tracks the 19-ID focus cluster from closure-gap plan | Done | Added `src/siasa/readmodels/stakeholder_functional_closure.py` with `build_stakeholder_functional_closure_report(repo_root)` and integrated emission of `readmodels/stakeholder_functional_closure.json` in `src/siasa/runs/artifacts.py`; new tests `tests/unit/test_readmodels_stakeholder_functional_closure.py` and updated `tests/unit/test_run_artifacts.py` assert focus cluster closure (`covered=19`, `implemented=19`, `not_implemented=0`) | Turns stakeholder functional closure from ad-hoc analysis into a governed, reproducible run artifact that can be consumed in CI/readiness evidence |
| P2 planning-governance closure hardening: CI gate for stakeholder functional closure focus cluster | Make stakeholder functional closure release-relevant by enforcing the 19-ID focus cluster closure in CI and integrating it into the release readiness gate index | Done | Added `scripts/ci_stakeholder_closure_check.py` (fails if focus cluster coverage !=19 or unresolved IDs remain), wired into `.github/workflows/vmodel-ci.yml`; extended `src/siasa/readmodels/release_evidence.py` with gate `stakeholder_functional_focus_cluster_closed`; updated `tests/unit/test_readmodels_release_evidence.py` for 6-gate readiness index including the new closure gate | Prevents silent regression of stakeholder functional closure and elevates the closure claim from documentation to continuously enforced release governance |
| P2 planning-governance closure visibility: readiness UI stakeholder focus closure signal | Expose stakeholder functional closure (19-ID focus cluster) directly in readiness UX and persisted local GUI artifact payload so governance status is visible without digging into raw JSON | Done | Extended `src/siasa/gui/local_app.py` to load `readmodels/stakeholder_functional_closure.json`, pass `stakeholder_functional_closure_view_model` through payload/build path, render KPI card `Stakeholder Focus Closure`, and add panel `Stakeholder Functional Closure Focus Cluster` with covered/open counts and unresolved IDs; local GUI now writes `stakeholder_functional_closure.json` when available; updated `tests/unit/test_local_gui.py` assertions for payload + readiness visibility | Makes stakeholder-closure governance state first-class in operator-facing readiness review and aligns UI with CI-enforced closure gates |
| P4 operational readiness hardening: release failure-drill gate and playbook | Prove that release governance actually fails closed under negative scenarios (not only in happy-path checks) and codify recovery behavior | Done | Extended `src/siasa/readmodels/release_evidence.py` with override-capable assessment and `build_release_failure_drill_report(...)` that injects known-gap, traceability-risk, and stakeholder-focus-open scenarios; added `scripts/release_failure_drill_check.py`; wired drill step into `.github/workflows/vmodel-ci.yml`; added `tests/unit/test_readmodels_release_failure_drill.py`; expanded runbook `docs/verification/release-go-no-go-runbook.md` with mandatory failure drill and recovery rule | Converts release-governance confidence from static green-path evidence into explicit fail-closed behavior verification and repeatable pre-release drill discipline |
| P4 operational readiness extension: artifact-driven failure-drill pack | Produce reviewable scenario artifacts for baseline and injected no-go states so release-failure behavior can be inspected outside CI logs and reused in readiness/UI checks | Done | Added `scripts/build_release_failure_drill_pack.py` to emit `build/release_failure_drill/latest/release_failure_drill_summary.json` plus per-scenario JSON artifacts (`baseline`, `known_gap_injected`, `traceability_closure_at_risk_injected`, `stakeholder_focus_cluster_open_injected`); strengthened `tests/unit/test_readmodels_release_failure_drill.py` scenario assertions; runbook updated with artifact-pack command | Makes failure-drill outcomes portable and auditable as governed artifacts instead of transient console output only |
| P4 operational readiness GUI extension: failure-drill readiness scenario rendering | Make failure-drill outcomes directly visible in generated readiness pages for baseline and injected no-go scenarios | Done | Added `tests/unit/test_local_gui.py::test_readiness_view_renders_failure_drill_scenarios_with_expected_no_go_signals` to render all drill scenarios and assert expected signals (`no_go`, blockers, focus-cluster open IDs); added `scripts/build_release_failure_drill_gui_samples.py` to generate per-scenario GUI bundles under `build/release_failure_drill/gui_samples`; runbook updated with GUI-sample command | Converts drill evidence into directly inspectable operator-facing readiness HTML, not only JSON/script logs |
| P1 validation interpretation extension: replay-attention watchlist scoping controls | Improve analyst actionability on non-perfect replay cases by adding interactive filters to the replay attention watchlist (attention level, owner, reason) with visible-row feedback | Done | `src/siasa/gui/local_app.py` now renders watchlist filter controls (`replay-attention-level-filter`, `replay-attention-owner-filter`, `replay-attention-reason-filter`) and client-side filtering over tagged watchlist rows (`replay-attention-row`) with `Visible attention rows` counter; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Makes the growing replay-attention portfolio operationally scannable for analysts/project lead without manual table parsing |
| P1 validation interpretation extension: replay-attention watchlist sorting/reset controls | Improve replay-watchlist triage speed by adding deterministic sorting and one-click filter reset to the existing scoping controls | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-sort` (`default`, `level-desc`, `level-asc`, `country-asc`, `case-asc`) plus `replay-attention-reset`; client-side logic now supports ordered row re-rendering and reset-to-default behavior while retaining existing text filters and visible-row counter; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Reduces analyst effort for prioritization passes (high→low attention first) and rapid context switching between review slices |
| P1 validation interpretation extension: replay-attention watchlist CSV export | Improve handoff/reporting utility by allowing analysts to export the currently visible (filtered) replay-attention rows as a CSV snapshot directly from the validation view | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-export-csv` and `exportVisibleReplayAttentionCsv()`; export uses the visible table rows after active filters/sort and downloads `replay_attention_watchlist.csv` with explicit watchlist columns; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Enables fast external triage sharing from governed validation output without manual copy/paste |
| P1 validation interpretation extension: replay-attention quick presets | Reduce repetitive manual filtering by adding one-click preset slices for common replay-attention review workflows (high severity, governance ownership, domain-gap cases) | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-preset` with values `high-only`, `governance-only`, `domain-gap-only` and `none`; preset selection auto-fills existing level/owner/reason filters and applies them immediately; manual filter edits auto-clear preset back to `none`; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Speeds recurrent triage loops and keeps review behavior consistent across analysts/project-lead checks |
| P1 validation interpretation extension: replay-attention export naming metadata | Improve downstream traceability of exported watchlists by encoding active preset/sort context directly into the CSV filename | Done | `src/siasa/gui/local_app.py` now composes export filenames as `replay_attention_watchlist_<preset>_<sort>.csv` (fallback preset `all`, sort from active control), while keeping export content scoped to currently visible rows; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Makes exported review artifacts self-describing and reduces manual relabeling in external handoff workflows |
| P1 validation interpretation extension: replay-attention copy-to-clipboard CSV | Improve rapid analyst handoff by enabling direct clipboard copy of the currently visible replay-attention CSV view with explicit copy-status feedback | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-copy-csv`, `replay-attention-copy-status`, `copyVisibleReplayAttentionCsv()`, and shared CSV text builder logic for visible rows; status messages cover success/failure/unavailable clipboard API paths; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Reduces friction when sharing triage slices into chat/tickets without intermediate files |
| P1 validation interpretation extension: replay-attention visible severity breakdown | Improve instant triage awareness by showing dynamic visible-row severity distribution (high/medium/low) next to the current filtered row count | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-visible-breakdown` and updates `applyReplayAttentionFilters()` to compute and render visible high/medium/low counts after every filter/preset/sort action; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`259 passed`) | Gives project lead and analysts immediate risk-shape feedback without manual scanning of the full table |
| P1 validation interpretation extension: replay-attention shareable filter-link state | Improve asynchronous collaboration by allowing analysts to share deep links that reopen validation view with the same replay-attention filter/sort/preset state | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-copy-link` and `replay-attention-link-status`; JS now persists replay-attention state into URL hash (`#ra=...` with `ra_level`, `ra_owner`, `ra_reason`, `ra_sort`, `ra_preset`) via `persistReplayAttentionStateToHash()`, restores state on load via `applyReplayAttentionStateFromHash()`, and copies a share URL via `copyReplayAttentionFilterLink()`; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass | Enables deterministic handoff of the exact analyst triage slice without manual filter reconstruction |
| P1 validation interpretation extension: replay-attention free-text watchlist search | Improve triage speed by adding a single free-text search over visible watchlist row content (country/case/reason/action) while preserving existing structured filters | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-text-filter` and extends `applyReplayAttentionFilters()` with row-level text matching (`rowText`) combined with level/owner/reason filters; URL-hash share state now includes `ra_text`; reset/preset flows clear text search for deterministic behavior; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass | Enables fast ad-hoc narrowing (e.g., specific case IDs or action phrases) without repeatedly composing multiple structured filters |
| P1 validation interpretation extension: replay-attention stale copy-status reset on filter state change | Prevent misleading clipboard/link success messages from persisting after filters/presets/sort change by clearing copy/link status whenever watchlist visibility is recomputed | Done | `src/siasa/gui/local_app.py` now adds `clearReplayAttentionCopyStatuses()` and invokes it at the start of `applyReplayAttentionFilters()` so copy-feedback reflects the current state slice only; tests updated in `tests/unit/test_local_gui.py` to assert helper + invocation; targeted GUI tests and full suite pass | Removes stale UX feedback that could otherwise suggest a copied artifact still represents the currently visible triage slice after scope changes |
| P1 validation interpretation extension: replay-attention active-filter summary badge | Improve screenshot/chat handoff clarity by rendering a live concise summary of the currently active replay-attention slice (preset + filters + non-default sort) directly next to row-count indicators | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-active-state` and helper `renderReplayAttentionActiveState(level, owner, reason, text, sortKey, preset)`; `applyReplayAttentionFilters()` now computes current preset and updates badge text (`Active: default` fallback) after each recomputation; tests updated in `tests/unit/test_local_gui.py` to assert the new control ID and helper hook; targeted GUI tests and full suite pass | Makes visible triage scope self-describing in UI captures and reduces ambiguous handoff when filters are active |
| P1 validation-to-action closure: replay-attention → annotation draft prefill handoff | Remove analyst action gap by enabling one-click transition from replay-attention cases to prefilled annotation drafts that preserve triage context | Done | `src/siasa/gui/local_app.py` now adds per-row action link `replay-attention-create-annotation` (`Create Annotation Draft`) to `annotations.html` with query prefill context (`country_id`, `case_id`, `attention_reason`, `owner_hint`, `suggested_next_action`, `attention_level`, `linked_item`) and extends `prefillAnnotationFromQuery()` to prefill linked items, severity mapping, tags, and draft text summary; tests updated in `tests/unit/test_local_gui.py` for validation-link/query hooks and annotation prefill logic strings; targeted GUI tests and full suite pass | Converts replay watchlist from interpretation-only into direct follow-up execution flow with deterministic handoff context |
| P1 validation-to-action extension: confidence-aware annotation prefill from replay evidence | Improve draft quality/consistency by auto-mapping replay evidence tier and review verdict into annotation confidence/tags during watchlist handoff | Done | `src/siasa/gui/local_app.py` now extends `replay-attention-create-annotation` links with `replay_evidence_tier` + `review_verdict` params and updates `prefillAnnotationFromQuery()` with `confidenceByReplayTier` mapping (`verified->high`, `strong->medium`, `weak->low`) plus additional prefilled tags for evidence tier and replay verdict; tests updated in `tests/unit/test_local_gui.py` for new query params and prefill hook strings | Reduces manual prefill variance and aligns draft confidence/taxonomy with governed replay evidence semantics |
| P1 validation-to-action robustness gate: replay-attention handoff context integrity | Add a deterministic integrity gate for replay-attention prefill context so missing handoff parameters are explicitly surfaced and complete contexts are positively acknowledged | Done | `src/siasa/gui/local_app.py` now factors prefill parsing into `collectReplayAttentionPrefillContextFromQuery()`, validates required replay-attention fields via `validateReplayAttentionPrefillContext(...)`, and emits explicit workflow status for missing fields (`Replay-attention prefill missing fields: ...`) or complete contexts (`Prefilled replay-attention workflow for ...`); tests updated in `tests/unit/test_local_gui.py` for helper hooks and status messages | Turns validation→annotation handoff from implicit best-effort behavior into an explicit integrity-checked workflow with operator-visible gate feedback |
| P1 validation-to-action extension: review-status-aware annotation prefill from replay verdict | Improve handoff actionability by deterministically pre-setting annotation review status from replay verdict so triage intent is preserved without manual dropdown edits | Done | `src/siasa/gui/local_app.py` now maps replay verdict to review status in `prefillAnnotationFromQuery()` via `reviewStatusByVerdict` (`pass->reviewed`, `warning->draft`, `fail->rejected`, `insufficient_data->unreviewed`) and applies it to `annotation-review-status-input`; tests updated in `tests/unit/test_local_gui.py` to assert the new mapping hook strings | Reduces manual inconsistencies in validation→annotation follow-up and makes replay verdict semantics immediately actionable in draft governance state |
| P1 validation-to-action extension: replay evidence score + domain-match context handoff | Preserve replay triage evidence fidelity in annotation drafts by forwarding numeric replay evidence score and domain-match ratio into annotation prefill context and summary text | Done | `src/siasa/gui/local_app.py` now extends replay-attention `Create Annotation Draft` links with `replay_evidence_score` and `domain_match_ratio`, parses them in `collectReplayAttentionPrefillContextFromQuery()`, and appends deterministic replay-evidence summary fragments (`tier`, `score`, `domain_match_ratio`) into the prefilled annotation text; tests updated in `tests/unit/test_local_gui.py` to assert new query params and parsing/summary hooks | Reduces analyst context loss across validation→annotation handoff and improves auditability of why a follow-up draft was created |
| P1 validation-to-action extension: domain-gap context handoff into annotation tags and summary | Preserve domain-gap semantics across validation→annotation transition by forwarding missing/unexpected domain lists and materializing them in deterministic annotation tags and replay-evidence summary text | Done | `src/siasa/gui/local_app.py` now extends replay-attention handoff links with `missing_expected_domains` and `unexpected_observed_domains`, parses both fields in `collectReplayAttentionPrefillContextFromQuery()`, enriches tag generation with `missing_<domain>` / `unexpected_<domain>`, and appends both domain-gap lists to replay evidence summary text in prefilled annotations; tests updated in `tests/unit/test_local_gui.py` for query params, parser hooks, split/tag hooks, and summary fragments | Reduces analyst context loss for gap-driven follow-up and improves auditability of which domain discrepancies triggered annotation action |
| P1 validation interpretation extension: enriched replay-attention CSV export with handoff evidence fields | Improve downstream triage/report utility by exporting visible replay-attention rows with deterministic handoff evidence columns (score, match ratio, domain-gap lists) instead of table-only fields | Done | `src/siasa/gui/local_app.py` now adds `replayAttentionCsvColumnsFromRow(...)` to extract per-row handoff query params from `Create Annotation Draft` links and uses a unified 13-column CSV schema (`...replay_evidence_score,domain_match_ratio,missing_expected_domains,unexpected_observed_domains...`) in both export and clipboard CSV paths; tests updated in `tests/unit/test_local_gui.py` for helper/header/param-hook coverage | Keeps exported watchlists aligned with validation→annotation handoff semantics and reduces context loss when triage moves to tickets/chat/spreadsheets |
| P1 validation interpretation extension: replay-attention verdict/tier filter controls | Improve replay triage precision by adding direct filters for replay verdict and replay evidence tier, including share-link/hash persistence and deterministic reset/preset behavior | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-verdict-filter` + `replay-attention-tier-filter`, extends `applyReplayAttentionFilters()` matching logic with verdict/tier checks, includes `ra_verdict` and `ra_tier` in hash persistence/restore/share, and clears the new filters in preset/reset flows; tests updated in `tests/unit/test_local_gui.py` for control IDs and filter/hash hooks | Reduces manual scanning effort when analysts need to isolate weak-evidence or mismatch verdict cases during validation follow-up |
| P1 validation interpretation extension: replay-attention advanced triage presets for weak-evidence and mismatch slices | Reduce repetitive manual setup by adding one-click presets targeting high-risk replay subsets (warning+weak evidence and mismatch-driven domain-gap cases) | Done | `src/siasa/gui/local_app.py` now extends `replay-attention-preset` with `warning-weak-evidence` and `mismatch-only`, with deterministic field population for verdict/tier/reason and existing reset behavior; tests updated in `tests/unit/test_local_gui.py` for new preset values and preset-logic hooks | Speeds governance triage loops by making frequent high-risk review slices reproducible in one click |
| P1 validation interpretation extension: replay-attention visible verdict distribution summary | Improve immediate triage awareness by showing a live verdict distribution summary for the currently visible replay-attention slice (e.g. mismatch/warning/pass counts) next to severity counts | Done | `src/siasa/gui/local_app.py` now adds `replay-attention-visible-verdict-breakdown` and computes `verdictCounts` in `applyReplayAttentionFilters()` from visible rows (`rowVerdict`), rendering a deterministic sorted summary string; tests updated in `tests/unit/test_local_gui.py` for new control ID and verdict-breakdown hooks | Reduces manual scanning effort when governance decisions depend on replay verdict composition of the active filtered subset |
| P4 CI hermeticity hardening: release-evidence smoke gate no longer depends on ambient generated browser artifacts | Remove fresh-runner flakiness in `test_build_repo_release_gate_assessment_is_go_for_current_repo` by making its AP-07/AP-11 expectations deterministic and explicit instead of implicitly relying on pre-generated GUI/browser artifacts | Done | Updated `tests/unit/test_readmodels_release_evidence.py` to call `build_repo_release_gate_assessment(...)` with deterministic passing overrides for `stakeholder_e2e_ui_smoke` and `stakeholder_browser_e2e_acceptance`; root-cause reproduction confirmed in fresh clone/venv (`passed_gates=10/12`, failing gates: `stakeholder_e2e_ui_smoke_covered`, `stakeholder_browser_e2e_acceptance_covered`) and fixed with targeted + focused regression: `test_readmodels_release_evidence.py` (`2 passed`), `test_readmodels_release_failure_drill.py` + `test_readmodels_release_gate.py` (`3 passed`) | Keeps smoke gate assertions stable across clean CI runners while preserving explicit gate semantics and avoiding hidden dependency on local build side effects |
| P1 traceability origin-inference + lag-signals package | Raise source-origin groundwork from descriptive-only to evidence-backed inference by adding artifact-window first-observed source classification and dependency timing-lag signals | Done | `src/siasa/traceability/lineage.py` now carries optional `observed_at`; `src/siasa/runs/orchestrator.py` populates it from raw-record fetch time; `src/siasa/gui/local_app.py` now computes source dependency observed lag (minutes) and source-origin inference states (`earliest_observed_source_in_window`, `later_observed_source_in_window`, etc.) with explicit uncertainty text; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass | Enables concrete origin-groundwork decisions from governed artifacts instead of unresolved placeholders, supporting a capability status jump |
| P1 overview navigation hardening package (status + search + visible count) | Close global anomaly orientation usability gap by adding faster slicing and explicit visibility feedback in the world overview flow | Done | `src/siasa/gui/local_app.py` now adds `status-filter`, `country-search`, and `overview-visible-count` to overview controls and extends `applyOverviewFilters()` to combine priority/domain/time/status/search filtering with live visible-country count and enriched filter summary text; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass | Converts world overview from baseline controls to faster operator-grade navigation, enabling capability status jump to Done |
| P1 real-source access closure: governed core-focus-complete evidence gate | Close the remaining Real source access Partial status by adding explicit automated verification for the governed `RUN-LIVE-CORE-COMPLETE-001` bundle (`release_verdict`, source failures, and country coverage) | Done | Added `tests/unit/test_core_focus_complete_probe_readmodels.py` to assert `readiness.json` is `release_verdict=ready` with `known_gaps=[]`, `system_status.json` is `run_status=success` with `failed_sources=[]`, and `world_map.json` covers the full 11-country `core-focus-complete` set (`UKR,RUS,CHN,TWN,IRN,ISR,TUR,IND,PAK,GEO,POL`); targeted test pass (`3 passed`) and full suite pass (`262 passed`) | Converts real-source-access evidence from narrative-only to executable closure checks against governed live artifacts |
| P1 validation/backtest closure: governed replay-depth evidence gate | Close the remaining Validation/Backtest Partial status by adding explicit automated verification for governed replay-depth portfolio evidence (multi-case summary, reference-library breadth, replay-depth scoring fields) | Done | Added `tests/unit/test_validation_replay_depth_probe_readmodels.py` to assert governed `build/run_artifacts/_rep_validation_replay_depth_probe_1/readmodels/validation_backtest.json` contains a 4-case `portfolio_summary` across `ISR,POL,TWN,UKR`, a broad `reference_case_library_summary` (>=20 cases including `UKR,GEO,PAK`), and replay-depth evidence metrics (`historical_replay_summary` + per-case `replay_evidence_score/tier` and source/provenance coverage ratios); targeted test pass and full suite pass (`266 passed`) | Converts validation depth evidence from narrative-only claims into executable regression checks against governed artifacts |
| P1 analyst cockpit closure: overview state persistence and shareable exploratory slices | Close the remaining Rich analyst cockpit Partial status by making analyst exploration stateful/reproducible (save/restore/share) across overview filters and view mode in the shipped GUI | Done | `src/siasa/gui/local_app.py` now adds `overview-save-state`, `overview-restore-state`, `overview-copy-link`, and `overview-link-status`; overview controls now support `getOverviewState()`, local save/restore via `localStorage`, and shareable URL-hash state roundtrip (`persistOverviewStateToHash`, `applyOverviewStateFromHash`, `ov_priority/ov_domain/ov_time/ov_status/ov_search/ov_view`); tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests (`15 passed`) and full suite pass (`266 passed`) | Converts cockpit interaction from session-ephemeral filtering to reproducible analyst workflows and sharable exploratory context |
| P2 requirements+verification sharpening: repo-wide traceability integrity gate | Strengthen governed closure truth by adding one machine-checkable global integrity report that enforces SwR baseline coverage, mapping completeness, and per-slice hygiene in one place | Done | Added `build_traceability_integrity_report(...)` in `src/siasa/traceability/consistency.py` and export in `src/siasa/traceability/__init__.py`; the report now computes SwR baseline requirement count vs mapped count, missing/orphan mappings, unhealthy slices, and repo closure at-risk aggregate; added `test_build_traceability_integrity_report_is_globally_clean` in `tests/unit/test_traceability_consistency.py` to gate `45/45` SwR mapping and `0` unhealthy slices/at-risk closure; targeted traceability tests and full suite pass (`266 passed`) | Moves from per-slice spot checks to a single global regression gate for requirement-to-implementation verification integrity |
| P4 demo/release readiness hardening: machine-readable go/no-go gate | Make release readiness operationally consumable for steering and automation by emitting an explicit go/no-go decision readmodel that combines readiness verdicts, unsuppressed gaps, and traceability integrity health | Done | Added `src/siasa/readmodels/release_gate.py` (`build_release_gate_view_model`) and export in `src/siasa/readmodels/__init__.py`; run artifact generation now emits `readmodels/traceability_integrity.json` and `readmodels/release_gate.json` via `src/siasa/runs/artifacts.py`; tests added in `tests/unit/test_readmodels_release_gate.py` and updated in `tests/unit/test_run_artifacts.py` to verify gate verdict/blockers and artifact generation | Converts demo/release readiness from implicit interpretation into an explicit machine-checkable gate usable for release steering and CI-style evidence checks |
   149|| P4 demo/release readiness UX closure: readiness page gate-verdict visibility | Make go/no-go steering directly visible in the analyst GUI by exposing gate verdict and blocker list on `readiness.html`, plus persisted `release_gate.json` handling in artifact-loaded site builds | Done | `src/siasa/gui/local_app.py` now loads optional `readmodels/release_gate.json`, computes fallback gate verdict when absent, renders `Gate Verdict` KPI + `Release Gate Blockers` panel in readiness view, and emits `release_gate.json` from local site generation; tests updated in `tests/unit/test_local_gui.py` (artifact payload + readiness page assertions); targeted GUI tests and full suite pass (`269 passed`) | Closes the operator-facing loop from machine gate computation to directly consumable release steering in the shipped GUI |
| P4 demo/release productization: release package page and machine-readable package artifact | Bundle the readiness, gate, traceability, validation, and operability signals into a single reviewable release/demo package surface for stakeholder review | Done | `src/siasa/readmodels/release_demo_package.py` now builds `release_demo_package.json` plus a rendered `release_package.html`; `src/siasa/gui/local_app.py` loads/exports the new package, adds a navigation entry, and renders prioritized items, demo sequence, and evidence bundle sections; `src/siasa/runs/artifacts.py` persists `readmodels/release_demo_package.json`; tests updated in `tests/unit/test_local_gui.py`; targeted GUI tests and full suite pass (`454 passed`) | Gives the project lead and stakeholders a single curated release/demo bundle instead of reconstructing the review path from multiple separate pages/artifacts |
| P4 operational release-control GUI extension: dedicated release failure-drill page | Surface release failure-drill outputs as a dedicated review page in the local GUI so operators can inspect baseline and no-go drill results without digging through the broader release package | Done | `src/siasa/gui/local_app.py` now loads `readmodels/release_failure_drill_report.json`, wires `release_failure_drill.html` and the associated JSON artifact into the local site build, and adds a dedicated navigation entry; `tests/unit/test_local_gui.py` now covers loader, export, and page rendering for the new drill page; targeted GUI tests and full suite pass (`18 passed`, `461 passed`) | Makes failure-drill governance a first-class, directly reviewable operator surface rather than a readiness-side detail |
| P4 operational release-control closure: CI gate + evidence pack + runbook | Make release readiness operationally enforceable and auditierbar by binding go/no-go into CI, generating a standard evidence pack, and documenting an explicit runbook decision flow | Done | Added `src/siasa/readmodels/release_evidence.py` with repo-level gate assessment and markdown summary helpers; added scripts `scripts/ci_release_gate_check.py` (CI fails on `no_go`) and `scripts/build_release_evidence_pack.py` (writes JSON+MD evidence pack); updated `.github/workflows/vmodel-ci.yml` to enforce the gate and build evidence artifacts; added runbook `docs/verification/release-go-no-go-runbook.md` and tests in `tests/unit/test_readmodels_release_evidence.py` | Converts readiness from capability-closure status into enforceable release operations with reproducible evidence and explicit decision protocol |

| AP-15 capability-vs-readiness operator split in release evidence output | Make operator steering unambiguous by exposing explicit dual KPI (`capability_fulfillment_percent` vs `release_readiness_index_percent`) plus divergence warning and signed gap in the same release evidence payload | Done | Extended `src/siasa/readmodels/release_evidence.py` to compute capability fulfillment from `docs/plans/siasa-project-lead-capability-matrix.md` via `functional_fulfillment`, emit `capability_fulfillment_percent`, `release_readiness_index_percent`, and `capability_vs_readiness` (`metrics_diverged`, `metric_gap_percent`, `operator_warning`, source); markdown evidence now renders these fields; tests expanded in `tests/unit/test_readmodels_release_evidence.py` and regression/full suite revalidated | Prevents false release-go interpretation when capability closure remains high but operational gates regress, and keeps project-lead decisions metric-causal |
| AP-17 operator-focused readiness drill dashboard slice | Compress scenario-by-scenario failure localization into stable operator action clusters so repeated no-go patterns can be triaged without scanning every scenario payload | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_failure_drill_digest` in `build_release_failure_drill_report(...)` (cluster_count, clusters with scenario_count/scenario_ids/remediation_hint, top_cluster_gate_id, operator_next_action), while preserving baseline-localization invariants; tests expanded in `tests/unit/test_readmodels_release_failure_drill.py`; targeted + full suite revalidated | Reduces operator triage latency by converting verbose drill evidence into an immediately actionable remediation-cluster digest |
| AP-18 readiness/operator UX integration in local GUI | Surface AP-16/AP-17 operator steering signals directly in `readiness.html` so release no-go triage is actionable without reading raw JSON artifacts | Done | Extended `src/siasa/gui/local_app.py` readiness renderer with panel "Operator Release Steering (AP-16/AP-17)" (failed-gate count, next action, AP-16 failed-gate table, AP-17 cluster summary table); payload loader now consumes optional `readmodels/release_evidence_assessment.json` and `readmodels/release_failure_drill_report.json`; `build_local_mvp_site(...)` exports operator summary/digest JSON when present; tests expanded in `tests/unit/test_local_gui.py` and run-artifacts compatibility revalidated | Moves operator release steering from backend-only evidence into directly consumable GUI workflow, reducing cognitive/latency overhead in incident triage |
| AP-19 release-failure-drill trend baseline | Convert AP-17 point-in-time failure clusters into a deterministic trend baseline so recurring no-go patterns can be tracked over subsequent snapshots | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_failure_drill_trend_baseline` derived from `operator_failure_drill_digest` (`snapshot_count`, `time_window`, ranked `trend_rows`, `top_recurring_gate_id`, `operator_focus`); each row stamps baseline recurrence metadata (`trend_status=baseline_established`, `trajectory=steady`, `recurrence_ratio=1.0`) for future drift comparison; tests extended in `tests/unit/test_readmodels_release_failure_drill.py`; targeted + full-suite revalidated | Establishes a stable reference frame for recurrence/worsening/improving analysis instead of one-off scenario inspection |
| AP-20 operator-focused stale-remediation closure drill | Enforce stale-remediation closure discipline by adding actionability+age guardrails that fail drill scenarios when unresolved stale actions breach SLA or stay non-actionable | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_stale_remediation_closure_drill` and guardrail evaluator (`_build_stale_remediation_closure_guardrails`) capturing closure requirement, SLA threshold (72h), breach count/reasons (`non_actionable_priority`, `sla_breach`) for baseline vs injected stale-gap scenario; drill checks now assert guardrail trigger behavior; tests extended in `tests/unit/test_readmodels_release_failure_drill.py`; targeted + full-suite revalidated | Converts stale-remediation from static presence checks to explicit closure-governance signals that surface overdue/non-actionable remediation risk |
| AP-21 trend+closure operator steering in readiness GUI | Surface AP-19 trend baseline and AP-20 stale-remediation closure guardrail signals directly in `readiness.html` so operator prioritization does not depend on raw drill JSON inspection | Done | Extended `src/siasa/gui/local_app.py` artifact payload loader to ingest `operator_failure_drill_trend_baseline` and `operator_stale_remediation_closure_drill` from `readmodels/release_failure_drill_report.json`; readiness renderer now shows AP-19 trend snapshot/top-gate table and AP-20 closure guardrail breach table; `build_local_mvp_site(...)` now exports `operator_failure_drill_trend_baseline.json` and `operator_stale_remediation_closure_drill.json` when present; tests extended in `tests/unit/test_local_gui.py`; targeted + full-suite revalidated | Completes operator steering loop by making trend+closure risk signals visible in primary readiness UX, reducing triage latency and context switching |
| AP-23 release drill delta ledger | Add deterministic snapshot-to-snapshot gate movement tracking and operator impact narrative so release-governance improvement/regression becomes auditable between runs | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_failure_drill_delta_ledger` plus optional `previous_report_override` support in `build_release_failure_drill_report(...)`; ledger compares prior vs current `operator_failure_drill_trend_baseline` rows, classifies `new_issue/improved/regressed/resolved/steady`, emits `movement_summary`, `top_regression_gate_id`, and `operator_impact_narrative`; `scripts/build_release_failure_drill_pack.py` now reuses the prior summary snapshot and persists both trend baseline and delta ledger for the next run; `scripts/release_failure_drill_check.py` now prints AP-23 fields; `src/siasa/gui/local_app.py` now loads/renders/exports `operator_failure_drill_delta_ledger`; targeted tests (`18 passed`), drill check passes, summary artifact `build/release_failure_drill/_ap23_probe/release_failure_drill_summary.json`, and GUI samples `build/release_failure_drill/_ap23_gui` generated | Closes the auditability gap between AP-22 prioritization and actual snapshot-to-snapshot movement by making regressions/improvements operator-visible and machine-readable |
| AP-24 operator remediation execution loop hooks | Connect AP-22 priorities and AP-23 movement rows to explicit action records so operator closure is machine-traceable and reviewable in drill artifacts and readiness GUI | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_remediation_execution_loop` derived from AP-22 priorities + AP-23 delta rows + gate diagnostics (`action_id`, `priority_rank`, `current_movement_status`, `execution_status`, `closure_target`, `closure_evidence_sources`, `scenario_ids`); `src/siasa/gui/local_app.py` now loads/renders/exports `operator_remediation_execution_loop`; `scripts/build_release_failure_drill_pack.py` and `scripts/release_failure_drill_check.py` now persist/print AP-24 execution-loop data; targeted tests (`18 passed`), drill check passes with `execution_loop_nonempty=true`, summary artifact `build/release_failure_drill/_ap24_probe/release_failure_drill_summary.json`, GUI samples `build/release_failure_drill/_ap24_gui`, and full suite revalidated (`289 passed`) | Closes the priority-to-action traceability gap by turning ranked remediation into explicit next-up/queued action rows with measurable closure targets and evidence sources |
| AP-25 veraltete Datenabdeckung in konkrete, schließbare Operator-Aktionen überführen | Turn stale-remediation closure breaches into an explicit operator action plan with deterministic next-up/queued actions, closure checks, and GUI-visible execution guidance | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_stale_remediation_action_plan` derived from AP-20 stale-remediation closure breaches; each action now carries clear `action_id`, `breach_reason`, `action_category`, `execution_status`, and `closure_check` (e.g. `priority_score > 0`, `unresolved_age_hours <= 72.0`); `src/siasa/gui/local_app.py` now loads/renders/exports the AP-25 stale-action plan; targeted tests (`18 passed`), full suite pass (`289 passed`), representative live probe `RUN-LIVE-REP-AP25-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`, `known_gaps=[]`, `release_verdict=ready`), and GUI bundle `build/local_gui/_rep_ap25_probe_1` was generated | Converts stale-remediation from a breach list into a directly executable operator work queue with explicit closure semantics |
| AP-26 Release-Blocker-Kette vereinfachen und kausal sichtbar machen | Separate primary root causes from derived release effects so operators can see which gate must actually be fixed first | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_blocker_causality` (`primary_root_cause_gate_id`, `root_cause_gate_ids`, `derived_gate_ids`, `causal_chain_rows`, `operator_next_action`); markdown evidence now emits a dedicated `## Blocker Causality` section; `src/siasa/gui/local_app.py` now loads and renders AP-26 root-cause vs derived-effect rows directly in `readiness.html`; targeted tests (`18 passed`), full suite pass (`289 passed`), representative live probe `RUN-LIVE-REP-AP26-002` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`, `known_gaps=[]`, `release_verdict=ready`), and GUI bundle `build/local_gui/_rep_ap26_probe_2` was generated | Converts the mixed blocker list into an operator-causal chain so release triage starts with the real cause instead of a derived red signal |
| AP-27 Stakeholder-Bedienpfade und Browser-Gates zu einem gemeinsamen Operability-Paket zusammenführen | Consolidate the five stakeholder-flow and browser governance gates into one operator-facing operability cluster so path health is readable as one coherent package | Done | Extended `src/siasa/readmodels/release_evidence.py` with `operator_operability_cluster` covering `stakeholder_e2e_flows_covered`, `stakeholder_e2e_ui_smoke_covered`, `stakeholder_browser_e2e_acceptance_covered`, `stakeholder_browser_interaction_depth_covered`, and `stakeholder_browser_failure_resilience_covered`; markdown evidence now emits `## Operability Cluster`; `src/siasa/gui/local_app.py` now loads/renders/exports the AP-27 cluster with gate group, cluster role, and pass/fail status; targeted tests (`18 passed`), full suite pass (`289 passed`), representative live probe `RUN-LIVE-REP-AP27-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`, `known_gaps=[]`, `release_verdict=ready`), and GUI bundle `build/local_gui/_rep_ap27_probe_1` was generated | Replaces several separate technical gate names with one operator-readable operability package for stakeholder flow and browser path health |
| P2 runtime robustness + breadth closure: mvp-complete (`30` countries) under real-source rate-limit pressure | Eliminate repeated mvp-complete live-run hangs/timeouts and make the 30-country governed run operationally deterministic with explicit outage-tolerance governance | Done | Added capped retry-delay handling to all live adapters (`max_retry_delay_seconds`), introduced large-set runtime profile for mvp-scale runs (reduced GDELT events export window, reduced GDELT doc payload, no full-fetch and pipeline retries at >=24 countries), added readiness filter for known global GDELT DOC outage signature, and adjusted low-signal Domain-B governance to `A/D` for `SAU`,`EGY`,`SDN`,`SWE`,`PAK`,`NLD`,`NZL`; tests pass (`253 passed`), live probe `RUN-LIVE-MVP-COMPLETE-014` completed within timeout (no hang), GUI generated (`build/local_gui/_mvp_complete_probe_14`), readiness now `known_gaps=[]`, `release_verdict=ready` | Closes the previously unstable 30-country tranche with explicit governed behavior under external rate limits and restores serial package closure capability at full MVP breadth |
| P3 governance transparency hardening: readiness known-gap suppression traceability | Keep readiness honesty while avoiding noisy outage-blocking by explicitly surfacing which known gaps were suppressed and why (instead of only returning filtered `known_gaps`) | Done | `src/siasa/readmodels/readiness.py` now publishes `suppressed_known_gaps` and `known_gap_suppression_reason` alongside `known_gaps`; added `tests/unit/test_readmodels_readiness.py` to verify suppression behavior for the mvp global-outage signature and non-mvp runs; targeted tests (`56 passed`) and full suite (`255 passed`) pass; representative probe `RUN-LIVE-REP-READINESS-TRUTH-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), GUI bundle `build/local_gui/_rep_readiness_truth_1` generated, readiness reports `known_gaps=[]`, `suppressed_known_gaps=[]`, `release_verdict=ready` | Improves operator/stakeholder explainability of readiness decisions without regressing outage-tolerant run closure |
| P3 governance transparency hardening: stale-coverage priority queue for operator actionability | Make stale data risk immediately actionable by ranking stale countries by stakeholder priority and staleness, and exposing this queue in system status and GUI | Done | `src/siasa/runs/artifacts.py` now emits `stale_priority_summary` + `stale_priority_watchlist` within `country_coverage_visibility`; `src/siasa/gui/local_app.py` renders a new "Stale Coverage Priority Queue" section in coverage surfaces; tests updated/added in `tests/unit/test_run_artifacts.py` and `tests/unit/test_local_gui.py`; targeted tests (`32 passed`) and full suite (`257 passed`) pass; representative probe `RUN-LIVE-REP-FRESH-QUEUE-002` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), readiness `known_gaps=[]`, `release_verdict=ready`, GUI `build/local_gui/_rep_fresh_queue_2` generated | Turns stale/freshness truth from passive visibility into a ranked remediation sequence for operators and project-lead steering |
| P3 role-based UI behavior: explicit viewer/analyst/admin GUI profiles | Turn role-behavior from conceptual requirement hooks into visible product behavior by gating navigation/pages according to role profile | Done | `src/siasa/gui/local_app.py` now supports `--ui-role` (`viewer`,`analyst`,`admin`) and role-gated page generation/navigation; viewer profile suppresses `annotations.html`, `reports.html`, and `runs.html` while analyst/admin retain full surfaces; new tests in `tests/unit/test_local_gui.py` cover viewer gating and invalid-role rejection; targeted GUI tests (`15 passed`) and full suite (`259 passed`) pass; representative probe `RUN-LIVE-REP-ROLE-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), readiness `known_gaps=[]`, `release_verdict=ready`, and dual GUI bundles were generated (`build/local_gui/_rep_role_1_analyst`, `build/local_gui/_rep_role_1_viewer`) | Closes the prior Weak role-behavior capability with concrete, test-verified, operator-visible role differentiation in the shipped local GUI |
| P3 report/export scoping UX: interactive filtering controls | Close the remaining report/export scoping gap by providing interactive filtering for governed report catalogs in the local GUI | Done | `src/siasa/gui/local_app.py` report view now includes scope controls (`report-type-filter`, `report-id-filter`) and dynamic visible-row counting over `report-row` entries; tests updated in `tests/unit/test_local_gui.py`; targeted tests (`15 passed`) and full suite (`259 passed`) pass; representative probe `RUN-LIVE-REP-REPORT-SCOPE-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), readiness `known_gaps=[]`, `release_verdict=ready`, GUI `build/local_gui/_rep_report_scope_1` generated | Closes the previous Reports/Exports Partial status by making inspect/scope workflows operationally interactive in the shipped GUI baseline |
| P3 domain deep-dive scoping UX: interactive feature/source filters | Close the remaining domain deep-dive interpretation gap by adding interactive feature/source scoping controls to domain detail views | Done | `src/siasa/gui/local_app.py` domain detail view now includes `domain-feature-filter` + `domain-source-filter` controls, visible row counters, and filtered feature/source tables (`domain-feature-row`, `domain-source-row`) with `applyDomainFilters` behavior; tests updated in `tests/unit/test_local_gui.py`; targeted tests (`15 passed`) and full suite (`259 passed`) pass; representative probe `RUN-LIVE-REP-DOMAIN-DEEP-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), readiness `known_gaps=[]`, `release_verdict=ready`, GUI `build/local_gui/_rep_domain_deep_1` generated | Closes the previous Domain deep dive Partial status by making per-domain inspection and interpretation scoping operationally interactive in the current governed GUI baseline |
| P1 source-lineage visualization: provenance flow and spread snapshots on analytics page | Make the dependency/provenance/epidemiology data visually scannable in the analytics page | Done | Added `analytics-source-lineage-visualization` section in `src/siasa/gui/local_app.py` with provenance flow badges, root/leaf flow arrow, and spread/amplification snapshots; analytics controls now deep-link to the section and tests cover rendered anchors/ids | Turns the source-lineage signals from table-only evidence into an analyst-friendly visual summary without adding new backend artifacts |
| P2 live-runtime breadth extension: focus-complete subset (`UKR`, `RUS`, `CHN`, `TWN`, `IRN`, `ISR`, `TUR`, `IND`, `PAK`, `GEO`, `POL`, `USA`, `DEU`, `EST`, `FIN`, `SAU`, `QAT`, `EGY`, `NGA`, `SDN`, `MMR`) | Consolidate core-focus-complete and extended-focus-complete into one governed 21-country pilot set with stable readiness evidence, while governing low-signal Domain-B countries (`GEO`, `EST`, `FIN`, `QAT`, `MMR`) as `A/D` where required for readiness honesty | Done | Runtime now supports `--pilot-set focus-complete`; expected domains in this consolidated scope are governed as `GEO:A/D`, `EST:A/D`, `FIN:A/D`, `QAT:A/D`, `MMR:A/D` (others unchanged); targeted runtime tests and full suite pass; live probe `RUN-LIVE-FOCUS-COMPLETE-003` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=21/21`), GUI bundle `build/local_gui/_focus_complete_probe_3` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes a larger unified P1+P2 governed runtime subset and proves stable ready-state execution at 21-country scope |
| P2 live-runtime breadth extension: extended-focus complete subset (`USA`, `DEU`, `EST`, `FIN`, `POL`, `SAU`, `QAT`, `EGY`, `NGA`, `SDN`, `MMR`) | Consolidate the previously closed extended-focus slices into one governed complete pilot set with stable readiness evidence, while governing `EST` and `MMR` expected domains as `A/D` to avoid misleading transient Domain-B gaps | Done | Runtime now supports `--pilot-set extended-focus-complete`; consolidated supported-country mappings include all eleven extended-focus countries for GDELT DOC/EVENTS and World Bank, expected domains are governed as `USA:A/B/D`, `DEU:A/B/D`, `EST:A/D`, `FIN:A/D`, `POL:A/B/D`, `SAU:A/B/D`, `QAT:A/D`, `EGY:A/B/D`, `NGA:A/B/D`, `SDN:A/B/D`, `MMR:A/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-EXT-FOCUS-COMPLETE-005` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=11/11`), GUI bundle `build/local_gui/_ext_focus_complete_probe_5` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes consolidated extended-focus runtime breadth as one reusable governed set and hardens readiness honesty for low-signal Domain-B countries |
| P2 live-runtime breadth extension: control/reference complete subset (`NOR`, `CHE`, `SWE`, `NLD`, `IRL`, `PRT`, `NZL`, `CAN`, `AUS`) | Consolidate the three control/reference slices into one governed complete pilot set with stable readiness evidence, while governing `NOR` and `PRT` expected domains as `A/D` to avoid misleading transient Domain-B gaps | Done | Runtime now supports `--pilot-set control-reference-complete`; consolidated supported-country mappings include all nine control/reference countries for GDELT DOC/EVENTS and World Bank, expected domains are governed as `NOR:A/D`, `CHE:A/B/D`, `SWE:A/B/D`, `NLD:A/B/D`, `IRL:A/B/D`, `PRT:A/D`, `NZL:A/B/D`, `CAN:A/B/D`, `AUS:A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-CTRL-REF-COMPLETE-003` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=9/9`), GUI bundle `build/local_gui/_control_reference_complete_probe_3` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes consolidated control/reference runtime breadth as one reusable governed set and hardens readiness honesty for low-signal Domain-B countries |
| P2 live-runtime breadth extension: control/reference third subset (`NZL`, `PRT`, `IRL`) | Add a third explicit control/reference runtime slice with governed mappings, pilot-set support, tests, and live evidence, while governing `PRT` expected domains as `A/D` to avoid a misleading transient Domain-B gap | Done | Runtime now supports `--pilot-set control-reference-third`; supported-country mappings now include `NZL`, `PRT`, `IRL` for GDELT DOC/EVENTS and World Bank, expected domains are governed as `NZL:A/B/D`, `PRT:A/D`, `IRL:A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-CTRL-REF-THIRD-002` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=3/3`), GUI bundle `build/local_gui/_control_reference_third_probe_2` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the third control/reference runtime breadth tranche and keeps readiness honest by explicitly scoping `PRT` away from unstable Domain-B expectations |
| P2 live-runtime breadth extension: control/reference broader subset (`NOR`, `CAN`, `AUS`) | Add a second explicit control/reference runtime slice with governed mappings, pilot-set support, tests, and live evidence | Done | Runtime now supports `--pilot-set control-reference-broader`; supported-country mappings now include `NOR`, `CAN`, `AUS` for GDELT DOC/EVENTS and World Bank, expected domains are governed as `NOR:A/D`, `CAN:A/B/D`, `AUS:A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-CTRL-REF-BROAD-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=3/3`), GUI bundle `build/local_gui/_control_reference_broader_probe_1` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the second control/reference runtime breadth tranche and extends governed live breadth with another stable P3-country control slice |
| P2 live-runtime breadth extension: control/reference initial subset (`CHE`, `NLD`, `SWE`) | Add the first explicit control/reference runtime slice beyond the current extended-focus clusters with governed mappings, pilot-set support, tests, and live evidence | Done | Runtime now supports `--pilot-set control-reference-initial`; supported-country mappings now include `CHE`, `NLD`, `SWE` for GDELT DOC/EVENTS and World Bank, expected domains are governed as `A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-CTRL-REF-INIT-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=3/3`), GUI bundle `build/local_gui/_control_reference_probe_1` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the first control/reference runtime breadth tranche and extends governed live breadth with a stable P3-country control slice |
| P2 live-runtime breadth extension: extended-focus crisis-initial subset (`NGA`, `SDN`, `MMR`) | Continue new-country extended-focus runtime expansion into a fragility/crisis tranche with governed mappings, pilot-set support, tests, and live evidence | Done | Runtime now supports `--pilot-set extended-focus-crisis-initial`; supported-country mappings now include `NGA`, `SDN`, `MMR` for GDELT DOC/EVENTS and World Bank, expected domains are governed as `A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-EXT-CRISIS-INIT-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=3/3`), GUI bundle `build/local_gui/_ext_crisis_init_probe_1` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the next new-country extended-focus runtime tranche and extends governed live breadth into the fragility/crisis cluster |
| P2 live-runtime breadth extension: extended-focus energy-initial subset (`SAU`, `QAT`, `EGY`) | Add the first truly new-country extended-focus runtime slice beyond previously supported countries, with governed mappings, pilot-set support, tests, and live evidence | Done | Runtime now supports `--pilot-set extended-focus-energy-initial`; supported-country mappings now include `SAU`, `QAT`, `EGY` for GDELT DOC/EVENTS and World Bank, expected domains are governed as `A/B/D`; targeted runtime tests and full suite pass; live probe `RUN-LIVE-EXT-ENERGY-INIT-001` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=3/3`), GUI bundle `build/local_gui/_ext_energy_init_probe_1` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the first new-country extended-focus runtime integration tranche and materially extends governed runtime breadth into the Middle East energy cluster |
| P2 live-runtime breadth extension: extended-focus broader subset (`USA`, `DEU`, `EST`, `FIN`, `POL`) | Extend governed live runtime breadth one step beyond `extended-focus-initial` with a controlled 5-country tranche and probe-driven resilience for transient GDELT DOC instability | Done | Runtime now supports `--pilot-set extended-focus-broader`; targeted runtime tests and full suite pass; default retry budget now grants one extra retry for this 5-country tranche to recover repeated isolated `SRC-GDELT-DOC` partial-success windows; live probe `RUN-LIVE-EXT-FOCUS-BROAD-002` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=5/5`), GUI bundle `build/local_gui/_ext_focus_broad_probe_2` was generated, and readiness reports `known_gaps=[]` with `release_verdict=ready` | Closes the next controlled broader-MVP/P2 runtime tranche with ready-state evidence and keeps hardening explicitly probe-driven |
| P2 live-runtime breadth extension: first extended-focus initial subset (`USA`, `DEU`, `EST`, `FIN`) | Extend governed live runtime beyond the core-focus subsets into a first explicit broader-MVP/P2 country tranche while preserving governed artifact/readiness outputs | Done | Runtime now supports `--pilot-set extended-focus-initial`; targeted runtime tests and full suite pass; follow-up hardening now narrows FIN expected live domains to governed `A,D` and adds an extended-set isolated-gap retry signature; live probe `RUN-LIVE-EXT-FOCUS-INIT-003` succeeded (`run_status=success`, `failed_sources=none`, `countries_with_updates=4/4`), GUI bundle `build/local_gui/_ext_focus_init_probe_3` was generated, and readiness now reports `known_gaps=[]` with `release_verdict=ready` | Closes the first beyond-core-focus runtime-breadth tranche in a ready state and reopens the next serial step as broader runtime breadth expansion |
| P1 validation interpretation extension: replay attention summary layer | Turn the initial replay attention watchlist into a more steerable analyst surface by aggregating non-perfect replay cases by attention level, reason, follow-up owner, and country concentration in both the governed readmodel and validation GUI | Done | `historical_replay_summary` now also carries `attention_level_counts`, `attention_reason_counts`, `attention_owner_counts`, and `attention_country_summary`; individual `attention_cases` now expose `owner_hint`; `validation.html` renders a Replay Attention Summary plus Attention by Country tables; targeted tests, full suite, and representative live probe confirm the richer interpretation layer | Makes replay follow-up more project-lead/analyst-usable by showing concentration and ownership patterns instead of only a flat watchlist |
| P1 validation interpretation extension: replay attention watchlist | Make non-perfect replay portfolios easier to review by surfacing the cases that need analyst follow-up, their reason, severity, and suggested next action in the governed validation readmodel and GUI | Done | `historical_replay_summary` now carries `attention_case_count` and ordered `attention_cases`; `validation.html` now renders a Replay Attention Watchlist; targeted tests, full suite, and representative live probe `RUN-LIVE-REP-VALATTN-001` confirm the new section for `VAL-ISR-2024-002` and `VAL-POL-2024-002` | Turns non-perfect replay portfolios into a more actionable analyst review surface instead of leaving them as raw verdict rows only |
| P1 validation breadth extension: add fragility/crisis P2 reference cases | Close the remaining obvious extended-focus validation-breadth tranche by adding `NGA`, `SDN`, and `MMR` as full A/B/D curated reference cases with fixture-backed and provider-backed archival replay inputs plus updated summaries and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-NGA-2024-001`, `VAL-SDN-2024-001`, and `VAL-MMR-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation/replay tests, full suite, representative live probe `RUN-LIVE-REP-VALP2-003`, and generated GUI `build/local_gui/_rep_valp2_probe_3/index.html` confirm `validation_backtest.json` now reports a 23-case / 21-country governed library with updated replay/reference summaries and `release_verdict=ready` | Closes the currently obvious P2 validation-breadth tranche and reopens the strategic choice between broader MVP/P2 runtime breadth and the next validation-depth / analyst-interpretation package |
| P1 validation breadth extension: add Middle East / energy P2 reference cases | Continue governed validation breadth beyond the first extended-focus tranche by adding `SAU`, `QAT`, and `EGY` as full A/B/D curated reference cases with fixture-backed and provider-backed archival replay inputs plus updated summaries and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-SAU-2024-001`, `VAL-QAT-2024-001`, and `VAL-EGY-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation/replay tests, full suite, representative live probe `RUN-LIVE-REP-VALP2-002`, and generated GUI `build/local_gui/_rep_valp2_probe_2/index.html` confirm `validation_backtest.json` now reports a 20-case / 18-country governed library with updated replay/reference summaries and `release_verdict=ready` | Extends governed validation into the next extended-focus regional tranche and narrows the immediate follow-on to the remaining fragility/crisis P2 countries versus switching focus back to broader runtime breadth |
| P1 validation breadth extension: add first P2 tranche reference cases | Extend governed validation beyond the now-complete core-focus set by adding a first extended-focus tranche (`USA`, `DEU`, `EST`, `FIN`) as full A/B/D curated reference cases with fixture-backed and provider-backed archival replay inputs plus updated summaries and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-USA-2024-001`, `VAL-DEU-2024-001`, `VAL-EST-2024-001`, and `VAL-FIN-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation/replay tests, full suite, and representative live probe confirm `validation_backtest.json` now reports a 17-case / 15-country governed library with updated replay/reference summaries | Pushes governed validation beyond the core-focus set into the first broader MVP/P2 tranche and reduces the next planning question to “continue P2 validation breadth” versus “switch to wider runtime breadth” |
| P1 validation breadth extension: add IND, IRN, and TUR reference cases | Close the remaining core-focus governed validation gap by adding full A/B/D curated reference cases for `IND`, `IRN`, and `TUR` with fixture-backed and provider-backed archival replay inputs plus updated summaries and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-IND-2024-001`, `VAL-IRN-2024-001`, and `VAL-TUR-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation/replay tests, full suite, and representative live probe confirm `validation_backtest.json` now reports a 13-case / 11-country governed library with updated replay/reference summaries | Closes the previously remaining core-focus validation-country gap and materially aligns curated governed validation evidence with the now-complete core-focus runtime subset |
| P1 validation breadth extension: add RUS and CHN reference cases | Extend the governed validation portfolio deeper into the already-supported core-focus runtime set by adding full A/B/D curated reference cases for `RUS` and `CHN` with fixture-backed and provider-backed archival replay inputs plus updated summaries and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-RUS-2024-001` and `VAL-CHN-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation/replay tests, full suite, and representative live probe confirm `validation_backtest.json` now reports `case_count=10` across 8 covered countries with updated replay/reference summaries | Narrows the remaining core-focus validation gap by bringing `RUS` and `CHN` into the curated governed portfolio and improves alignment between runtime-supported breadth and curated validation evidence |
| P1 validation breadth extension: expand curated library to PAK and GEO | Broaden the governed validation portfolio beyond the first 4-country library by adding `PAK` and `GEO` reference cases with fixture-backed and provider-backed archival replay inputs, updated summaries, and runtime-visible evidence | Done | `validation_reference_cases.yaml`, `validation_replay_inputs.yaml`, and `validation_archival_replay_manifest.yaml` now add `VAL-PAK-2024-001` and `VAL-GEO-2024-001`; governed archival bundles exist under `vmodel/verification/archival_replay_inputs/`; targeted validation tests, full suite, and representative live probe `RUN-LIVE-REP-VALBREADTH-001` confirm `validation_backtest.json` now reports `case_count=8` across 6 covered countries with updated replay/reference summaries | Moves validation breadth from the original 4-country library to a 6-country governed portfolio and better aligns validation evidence with the newer `core-focus-complete` runtime breadth |
| P1 validation depth extension: challenge/mismatch replay diversity | Move historical replay beyond perfect-match-heavy cases by adding governed partial-coverage and mismatch challenge cases to the curated validation library, archival manifest, replay inputs, summaries, and GUI evidence surface | Done | `validation_reference_cases.yaml` now includes additional governed challenge cases (`VAL-POL-2024-002`, `VAL-ISR-2024-002`), fixture and provider-backed archival replay inputs exist for them, archival-source validation now allows governed replay bundles to be a strict subset of case reference sources, and replay/reference summaries now expose mixed verdict/evidence distributions instead of only perfect matches; targeted tests, full suite, and governed runtime artifact checks pass | Makes the validation layer more realistic by proving the system can surface partial and mismatch replay outcomes honestly rather than only accumulating perfect replay examples |
| P1 validation depth extension: replay verdict depth enrichment | Move historical replay from simple match/mismatch reporting toward richer evidence interpretation by adding replay evidence score/tier and coverage-quality ratios to reviews, summaries, and GUI | Done | `historical_replay_reviews` now carry `replay_evidence_score`, `replay_evidence_tier`, `replay_source_coverage_ratio`, and `replay_provenance_completeness_ratio`; `historical_replay_summary` now aggregates average replay evidence metrics and tier counts; `validation.html` renders those fields; targeted tests, full suite, and governed runtime probe pass | Turns replay review from a binary-ish verification surface into a more analyst-usable evidence-quality interpretation layer |
| P1 validation depth extension: archival provenance governance hardening | Tighten governed archival replay rigor by validating manifest/bundle consistency and surfacing archival provenance metadata in replay readmodels and GUI | Done | Manifest loading now rejects duplicate/malformed provenance entries, archival bundles are checked against case-country and source-manifest consistency, `historical_replay_reviews` expose replay source/file metadata, and `historical_replay_summary` now carries replay input totals plus source-coverage counts; targeted tests, full suite, and governed runtime probe pass | Converts archival replay from “provider-backed and present” into a stricter governed evidence surface with explicit provenance/accountability signals |
| P1 validation depth extension: provider-backed archival replay completion | Move validation beyond pure repo-fixture replay by introducing a governed archival replay manifest, repo-stored archival replay inputs, and a provider-backed replay path for the full current curated library | Done | `validation_backtest.json` and `validation.html` now carry `provider_backed_archival_replay` for all four curated cases (`VAL-UKR-2022-001`, `VAL-POL-2023-001`, `VAL-ISR-2023-001`, `VAL-TWN-2024-001`); manifest/loader tests, full suite, and governed runtime probe now confirm `review_basis_counts = {provider_backed_archival_replay: 4}` | Closes the current mixed-basis gap and establishes a governed provider-backed archival replay surface across the full curated validation library |
| P1 validation depth extension: first true historical replay execution | Move validation beyond curated historical review by executing the analytical pipeline against fixture-backed historical replay inputs for the current curated reference-case library without overclaiming provider-backfilled archive replay | Done | `validation_backtest.json` and `validation.html` now carry `historical_replay_reviews` plus `historical_replay_summary` for all four current curated cases (`VAL-UKR-2022-001`, `VAL-POL-2023-001`, `VAL-ISR-2023-001`, `VAL-TWN-2024-001`); targeted tests, full suite, and governed runtime probe `RUN-LIVE-REP-VAL-008` pass | Turns the validation depth path from partial replay coverage into replay-derived outcomes across the full current curated library while keeping the remaining archive/backfill limitation explicit |
| P1 validation depth extension: historical reference review scoring | Extend the curated reference-case library into first historical reference-review / evidence-scoring outputs without pretending full runtime replay execution | Done | `validation_backtest.json` and `validation.html` now carry curated case-level historical review verdicts, evidence tiers, and average evidence score; targeted tests, full suite, and governed runtime validation artifact checks pass | Moves validation depth from pure inventory to explicit historical case interpretation while keeping the no-fake-replay boundary honest |
| P1 validation depth extension: curated reference-case library | Replace the thin single-case validation baseline with an explicit repo-backed reference-case inventory that is visible in governed validation artifacts and GUI review flows without overclaiming historical replay scoring | Done | `validation_backtest.json` and `validation.html` now carry a curated 4-case library (`UKR`, `POL`, `ISR`, `TWN`) plus summary counts/time range; targeted tests, full suite, and live probe `RUN-LIVE-CORE-COMPLETE-VAL-003` passed | Gives analysts a broader governed validation review surface and turns the validation flow from a thin support-check view into an explicit reference-case inventory |
| P2 live-runtime breadth extension: complete core-focus subset | Extend governed live runtime breadth beyond `core-focus-broader` by adding `PAK` and `GEO` as the remaining obvious core-focus countries in a new 11-country named subset, while pacing GDELT DOC more conservatively for very large multi-country runs | Done | Runtime now supports `--pilot-set core-focus-complete`, targeted tests and full suite pass, and live probe `RUN-LIVE-CORE-COMPLETE-001` completed successfully with 11 updated countries and `release_verdict=ready` | Closes the currently obvious core-focus breadth gap and demonstrates that the full current core-focus set can run successfully as one governed subset |
| P2 live-runtime breadth extension: broader core-focus subset | Extend governed live runtime breadth beyond `core-focus-expanded` by adding `IRN` and `TUR` as a new 9-country named subset while preserving honest `TWN` A/B scoping and reusing the existing broader-subset retry hardening | Done | Runtime now supports `--pilot-set core-focus-broader`, targeted tests and full suite pass, and live probe `RUN-LIVE-CORE-BROAD-001` completed successfully with 9 updated countries and `release_verdict=ready` | Broadens governed exploratory breadth by two additional P1 countries (`IRN`, `TUR`) and leaves `PAK` / `GEO` as the next obvious breadth candidates rather than stopping on the earlier 7-country slice |
| P2 live-runtime breadth extension: first core-focus-expanded subset | Extend governed live runtime breadth beyond `core-focus-initial` by adding `IND` as a seventh country in a reusable named subset while preserving honest `TWN` A/B scoping and making the existing `POL/B` transient-gap retry logic reusable for this broader subset | Done | Runtime now supports `--pilot-set core-focus-expanded`, targeted tests and full suite pass, `RUN-LIVE-CORE-EXP-003` completed successfully with 7 updated countries, and the broader subset now also has a successful `RUN-LIVE-CORE-EXP-RETRY-004` probe with `release_verdict=ready` after extra GDELT DOC recovery budget | Broadens governed exploratory breadth by one additional P1 country (`IND`) and now also has a repeatable ready-state recovery path instead of leaving the broader subset stuck behind transient GDELT DOC instability |
| P2 live-runtime hardening: broader GDELT DOC recovery budget | Allow a broader governed live subset to recover from repeated isolated `SRC-GDELT-DOC` partial-success attempts instead of giving up after a single rerun | Done | `run_governed_live_pipeline()` now uses an extra default retry for 7-country-and-broader subsets, targeted and full runtime tests pass, and live probes `RUN-LIVE-CORE-EXP-RETRY-004` and `RUN-LIVE-CORE-BROAD-001` confirm ready-state recovery on broader subsets | Improves practical stability of broader governed subsets without broadening retry behavior indiscriminately for smaller subsets |
| P2 live-runtime hardening: pipeline-level GDELT DOC recovery | Re-run the governed live pipeline after isolated GDELT DOC partial-success failure so the latest bundle can recover under real provider throttling | Done | The live runtime now retries the whole multi-country pipeline after isolated `SRC-GDELT-DOC` failure, targeted tests pass, and consecutive representative live probes completed with all four sources successful | Moves runtime stability from occasional success toward repeatable governed latest-bundle generation |
| P2 live-runtime hardening: pipeline-level GDELT EVENTS recovery | Re-run the governed live pipeline after isolated `SRC-GDELT-EVENTS` partial-success failure in multi-country runs so transient event-feed gaps are recovered consistently with the existing GDELT DOC recovery behavior | Done | `run_governed_live_pipeline()` now also retries when `failed_sources == ["SRC-GDELT-EVENTS"]`; targeted runtime test plus full suite pass; representative probe `RUN-LIVE-REP-RETRY-EVT-001` succeeded with `failed_sources=none`, `countries_with_updates=4/4`, and `release_verdict=ready` | Closes another concrete transient live-source failure mode and makes representative latest-bundle generation more stable without broad uncontrolled retry behavior |
| P2 live-runtime hardening: representative POL Domain-B recovery retry | Re-run the representative governed pipeline after the specific transient `POL / B / no_usable_input_data` pattern (`SRC-GDACS` zero records plus `SRC-GDELT-EVENTS` other-country-only window) so a single unstable event/disaster window does not freeze the latest bundle in a misleading released state | Done | The live runtime now retries exactly that representative-gap signature once, targeted tests pass, and consecutive representative probes now produced `POL` Domain B without country-gap rows | Narrows a concrete latest-bundle instability mode while keeping the retry scope explicitly tied to the observed representative pilot pattern |
| P2 live-runtime hardening: latest-bundle output hygiene | Ensure each governed run replaces stale bundle contents instead of leaving old reports/exports/readmodels behind in the same output directory | Done | Artifact generation now clears stale `snapshot.json`, `readmodels/`, `reports/`, and `exports/` before writing the new bundle; targeted cleanup regression passes and a fresh `latest` run now contains only `RUN-LIVE-REP-LATEST-022` artifacts | Removes misleading stale-bundle carryover so project-lead/runtime evidence reflects only the current run |
| P2 live-runtime hardening: annual macro freshness horizon | Stop annual World Bank macro inputs from being treated as false freshness failures under daily-style sufficiency thresholds | Done | World Bank records now carry an explicit annual freshness horizon, feature confidence preserves it, sufficiency evaluation respects it, targeted tests pass, and controlled live probes show Domain D can now remain D1 instead of collapsing to D0 | Removes one misleading insufficiency source and makes representative live probes materially more truthful |
| P2 live-runtime hardening: GDELT DOC query budget and pacing | Reduce representative multi-country runtime fragility from GDELT DOC rate limiting | Done | Live runtime now configures conservative per-country GDELT DOC record budgets and inter-request pacing, with targeted adapter/runtime tests and full-suite pass | Narrows one concrete live-source instability cause without overclaiming full runtime closure |
| P0 real-source-access readiness | Determine which real sources can be integrated honestly and under what constraints | Done | Source-access assessment exists; governed live-runtime pilot sets and evidence gates are in place (including `core-focus-complete` closure evidence and readiness-governed latest bundles) | Real-source integration readiness is now governed/operational for the current product scope; further source breadth is expansion, not readiness closure |
| P1 artifact-complete MVP | Ensure latest bundle contains the governed evidence artifacts needed for honest readiness claims | Done | Latest bundles now persist `validation_backtest`, explicit optional-artifact status, and a machine-readable `readmodels/readiness.json`; GUI artifact loads can also reuse the persisted readiness model | The remaining next-step focus shifts away from artifact completeness and toward broader runtime/source breadth or deeper analyst-facing validation content | Closes the basic governed readiness-evidence contract for the current pilot |
| P2 data-complete country coverage MVP | Move from narrow proof to representative multi-country runtime coverage | Done | Repo evidence now includes a representative live probe where all four countries update successfully, POL/TWN Domain B coverage is restored via widened GDELT Events aggregation, and TWN Domain D is formally scoped out of the governed pilot with country-specific expected-domain artifacts | Further breadth beyond the governed pilot is an expansion topic, not an inconsistency inside the current pilot |
| P3 visual analyst GUI MVP | Build practical GUI usability, controls, comparisons, and visual navigation | Done | Current GUI baseline includes role-based navigation, map and country/domain drill-downs, readiness/go-no-go visibility, traceability and coverage governance panels, report/validation controls, replay-attention filtering/sharing, and AP-04/AP-07 stakeholder flow visibility with persisted readiness artifacts | Analyst-facing static GUI MVP scope is functionally closed; future work is architectural/runtime enhancement rather than MVP usability closure |
| P4-WP-001 source-gap transparency overlays | Make coverage/freshness/gap remediation visible in overview/map/coverage flows | Done | Commit `8c3537d` and passing tests | Closed major transparency gap |
| P3-WP-005 annotation create/edit workflow | Make annotations operationally editable in the static local GUI | Done | Commit `884659b`, passing tests, browser verification | Closed major analyst workflow gap |
| P4-WP-002 richer baseline/historical overlays | Improve interpretation quality in trend/domain views | Done | Commit `8160ef2`, passing tests, full-suite pass, and browser verification confirm analyst-friendly baseline/historical summaries on trend and domain views | Closed the targeted interpretation gap for the current static GUI baseline |
| P4-WP-003 source-dependency / source-origin groundwork | Improve analyst visibility into coupled sources and make origin-inference gaps explicit | Done | Current work tree adds dependency cluster candidates and source-origin groundwork tables to traceability view, with passing tests, full-suite pass, and browser verification | Closed the intended groundwork slice without overclaiming full origin inference |
| AP-INT-01 Phase 4-6 analytical module pipeline integration | Wire rule_engine, cross_domain_fusion, probabilistic scoring, and uncertainty_propagation into DailyRunOrchestrator.run() so they are called on every run and results are materialized in DailyRunResult + artifact JSON | Done | Extended `src/siasa/runs/orchestrator.py`: rule_engine invoked per country/domain against `vmodel/project/assessment_rules.yaml`; `fuse_domain_evidence()` per country; `compute_bayesian_status()` per domain; `propagate_uncertainty()` per country; all defensive (try/except, skip if YAML absent); extended `src/siasa/runs/artifacts.py` with `_write_analytics_artifacts()` writing `analytics/rule_evaluations.json`, `cross_domain_fusion.json`, `bayesian_estimates.json`, `uncertainty_budgets.json`; new `DailyRunResult` fields: `rule_evaluation_results`, `fusion_results`, `bayesian_estimates`, `uncertainty_budgets`; added `test_daily_run_orchestrator_populates_analytical_module_results()` asserting all four fields populated; commit `61c5284`; targeted orchestrator tests `13 passed`, analytical unit tests `207 passed` | Converts previously standalone Phase 4-6 modules from dead code into active pipeline stages that run on every orchestrated data cycle and produce governed artifact outputs (req trace: AP-F22..F27, StR-029..061) |
| AP-INT-02 freshness_config YAML wiring into data_sufficiency | Wire governed per-domain/per-source freshness thresholds from `vmodel/project/freshness_config.yaml` into `evaluate_data_sufficiency()` threshold resolution so domain C/E features are evaluated against their actual governed horizon (e.g., C=336h) instead of the hardcoded 168h fallback | Done | Extended `src/siasa/scoring/data_sufficiency.py` with `_resolve_effective_freshness_threshold()` helper using `freshness_config.resolve_freshness_threshold()`; per-feature `freshness_horizon_hours` still takes priority; config lookup fully defensive; new test `test_evaluate_data_sufficiency_uses_freshness_config_yaml_when_no_per_feature_horizon` passes; commit `79e7094`; targeted `4 passed` + regression `38 passed` | Closes the hardcoded-168h gap for domain C (configured 336h) and any future source-specific overrides; req trace: AP-F11, StR-129..134 |
| AP-INT-03 dependency_graph/provenance_chain/info_epidemiology pipeline integration | Integrate remaining three standalone Phase 6 analysis modules into orchestrator pipeline and materialize as artifact JSON | Done | Extended `src/siasa/runs/orchestrator.py`: (e) build `DependencyEdge` from feature provenance_source_ids co-occurrence + `build_dependency_graph()`; (f) build `ProvenanceNode/Edge` from `lineage_records` + `build_provenance_chain()`; (g) build `SpreadObservation` from normalized_records freshness_hours + `detect_spread_paths()` + `detect_amplification()`; extended `artifacts.py` to write `analytics/dependency_graph.json`, `provenance_chain.json`, `info_epidemiology.json`; updated orchestrator test; commit `712fdcf`; targeted `123 passed` | Completes pipeline integration of all standalone Phase 6 modules; all 7 analytics artifacts now written to artifact bundle per run; req trace: AP-F23, AP-F24, AP-F25, StR-029..044 |
| AP-INT-04 analytics.html GUI page | Add dedicated Analytics page to the local GUI showing all 7 Phase 4-6 analytical module outputs (cross-domain fusion, Bayesian estimates, uncertainty budgets, rule evaluations, dependency graph, provenance chain, info epidemiology) loaded from analytics/ artifact JSONs | Done | Added `_render_analytics()` with 7 styled sections; extended `load_site_payload_from_artifacts()` to load analytics/ dir; added `analytics_view_model` param to `build_local_mvp_site()`; nav entry 🔬 Analytics; test `test_build_local_mvp_site_renders_analytics_page_when_view_model_provided` (1 passed, 5.5s); commit `61e893e`; targeted `80 passed` non-GUI tests | Makes Phase 4-6 analytical outputs first-class visible in the operator/analyst GUI — any run with analytics/ artifacts now auto-generates a navigable Analytics page; req trace: AP-INT-04, AP-F22..F27, StR-029..061 |
| AP-INT-05 live-runtime verification hardening under partial source degradation | Ensure `verify_latest_bundle` can validate a governed latest bundle even when runtime degrades to `partial_success` due single-source outages/timeouts, while keeping strict mode default | Done | Root cause reproduced in live run (`TimeoutError` from World Bank call had bubbled and latest verifier rejected `partial_success`); hardened `WorldBankIndicatorsAdapter.fetch()` to degrade gracefully on any provider/network exception; extended `verify_latest_bundle()` with explicit `allow_partial_success` + `allow_failed_sources` toggles (strict default unchanged); CLI `scripts/verify_latest_bundle.py` now exposes both flags; added regression tests for timeout handling and partial-success verification path; live evidence: `python -m siasa.runs.live_runtime --country-id UKR` => `run_status=partial_success`, then `scripts/verify_latest_bundle.py --allow-partial-success --allow-failed-sources` => `status: ok`; targeted tests `15 passed`, focused regression `86 passed` | Closes operational gap between graceful pipeline degradation and bundle verification, so governed evidence validation remains usable during transient source instability without weakening default quality gates |
| AP-INT-06 operational-latest dual-mode runtime gate (strict by default, explicit degraded mode) | Keep operational-latest bundle generation fail-closed by default, but provide explicit opt-in mode for degraded live runs so representative/multi-country operations can still publish a traceable bundle under transient source outages | Done | Extended `build_operational_latest_bundle()` with `allow_partial_success` and `allow_failed_sources` (both default `False`), split gate checks into status gate + failed-source gate, and kept strict behavior unchanged; added CLI flags in `scripts/build_operational_latest_bundle.py`; expanded tests in `tests/unit/test_operational_latest.py` for (a) default reject partial_success, (b) default reject failed_sources even with success status, (c) explicit allow-mode accepts degraded run and still writes GUI/history; CLI help verified includes both new flags; targeted tests `10 passed`, focused regression `67 passed` | Aligns operational run publication with explicit governance policy: strict mode remains the default quality gate, while degraded mode is intentional, visible, and auditable instead of ad-hoc failure bypass |
| AP-INT-07 CI runtime profile split (hermetic gate vs explicit live probe lane) | Separate fast deterministic CI gating from network-dependent governed live runtime probing, so main CI stays stable while live behavior is still executable and evidenced on-demand | Done | Refactored `.github/workflows/vmodel-ci.yml`: renamed primary gate job to `hermetic-smoke` (push/PR), added `workflow_dispatch` boolean input `run_live_probe`, and added dedicated `live-probe-governed-runtime` job that only runs when explicitly requested; live lane executes `scripts/build_operational_latest_bundle.py` with `--allow-partial-success --allow-failed-sources`, verifies bundle with degraded-aware verifier flags, and uploads run artifacts/logs as diagnostics | Establishes clear dual-lane governance: deterministic merge gate remains reliable, while live-network runtime checks are explicit, auditable, and operationally reproducible without destabilizing default CI |
| AP-INT-08 analytics cross-link/filter UX + role constraints | Improve analyst navigation inside `analytics.html` with direct section anchors and client-side section filtering, and enforce explicit role-based visibility boundaries for advanced dependency/provenance views | Done | Extended `_render_analytics()` in `src/siasa/gui/local_app.py`: each analytics block now has stable section IDs (`analytics-*`) + `data-role-min`; added top control panel (`analytics-controls`) with anchor quick-links and filter controls (`analytics-section-filter`, `analytics-text-filter`, reset) plus deterministic JS function `applyAnalyticsSectionFilter`; role boundaries set as viewer/analyst/admin per section (dependency/provenance admin-only). Updated `tests/unit/test_local_gui.py` to assert control IDs, anchors, filter function wiring, section IDs, and role tags. Targeted GUI tests `18 passed`; focused regression (`run_artifacts`/`run_orchestrator`/`governed_live_runtime`) `86 passed` | Closes practical discoverability and access-governance gap for Phase 4–6 analytics outputs: analysts can navigate/filter quickly while sensitive dependency/provenance internals are explicitly gated by role semantics |
| AP-INT-09 C/E live-runtime utilization hardening (structured C/E sources + fallback feature mapping) | Ensure Domains C and E are operationally consumed in governed live runs by adding stable C/E sources and aligning feature extraction with actual live signal keys, while preserving strict governance defaults | Done | Extended `src/siasa/runs/live_runtime.py`: added `UNHCRPopulationAdapter` (Domain C) and `CISAKEVAdapter` (Domain E) to governed adapter stack; added active normalization mappings `MAP-SRC-UNHCR-POP-v1` and `MAP-SRC-CISA-KEV-v1`. Extended `src/siasa/features/domain_c.py` with displacement-aware fallback (`displacement_total` / `humanitarian_report_count`) and explicit `C_displacement_total` feature. Extended `src/siasa/features/domain_e.py` with live-signal fallback mapping (`article_count`/`tone`, `cyber_kev_*`) for `E_cyber_mention_volume`, `E_info_ops_tone`, `E_tech_disruption_signals`. Updated runtime tests (`tests/unit/test_governed_live_runtime.py`) for new C/E adapter source IDs and feature tests (`test_features_domain_c.py`, `test_features_domain_e.py`) for fallback behavior. Verification: targeted pack `64 passed`; focused regression (`test_operational_latest.py`, `test_latest_bundle_verification.py`, `test_run_orchestrator.py`, `test_run_artifacts.py`) `42 passed`; representative live probe `RUN-LIVE-INT-09-001` succeeded (`run_status=success`, `failed_sources=none`) and produced C/E domain detail artifacts (e.g. `readmodels/domain_details/UKR__C.json`, `UKR__E.json`) | Closes practical C/E utilization gap between architectural inclusion and live-runtime signal reality: C/E are now fed by structured sources and fallback-compatible feature extraction paths, yielding governed, inspectable C/E artifacts in operational live runs |
| AP-INT-10 CI live-probe evidence digest readmodel | Add a compact, machine-readable governance summary for the dispatch-only live probe lane so operators can assess run status, failed sources, and effective C/E usage without opening multiple artifacts | Done | Added `src/siasa/readmodels/live_probe_evidence_digest.py` + CLI script `scripts/build_live_probe_evidence_digest.py` writing `readmodels/live_probe_evidence_digest.json` from live probe artifacts. Digest includes CI context, run context (`run_id`, `run_status`, `failed_sources`), per-country C/E evidence, utilization ratios, and governance verdict (`green/amber/red`) with operator next action. Inference uses concrete domain detail artifacts (`domain_details/<country>__C/E.json`) to avoid false negatives when `world_map.active_domains` is governance-filtered. CI workflow `.github/workflows/vmodel-ci.yml` live lane now builds this digest after verifier step and uploads it in diagnostics artifacts. Added unit tests `tests/unit/test_live_probe_evidence_digest.py`; verification: targeted+focused regression `61 passed`; sample digest generation on representative probe produced verdict `green` and `combined_ce_ratio=0.875`. | Makes the live lane auditable at a glance: one stable JSON summarizes operational health and actual C/E utilization evidence, improving release/demo governance without weakening strict defaults |
| AP-INT-11 CI live-probe digest policy gate | Add an explicit, configurable pass/fail governance gate for the live-probe lane based on the AP-INT-10 digest, so CI decisions are auditable and profile-tunable instead of implicit interpretation of raw artifacts | Done | Added policy evaluator `src/siasa/readmodels/live_probe_policy_gate.py` with thresholds for `min_combined_ce_ratio`, allowed governance verdicts, and `max_failed_sources`; added CLI gate script `scripts/ci_live_probe_digest_gate_check.py` (exit code 0/1 with machine-readable blockers); integrated new workflow step in `.github/workflows/vmodel-ci.yml` directly after digest build; added unit tests `tests/unit/test_live_probe_policy_gate.py` for pass/fail scenarios (verdict disallow, CE-ratio threshold miss, failed-source overflow). | Converts live-lane governance from best-effort artifact review into deterministic policy enforcement with explicit thresholds, preserving strict serial release discipline while keeping degraded-mode controls intentional and transparent |
| AP-INT-12 versioned live-probe governance policy profiles | Move live-probe gate thresholds from workflow CLI literals into a versioned policy-profile file so governance posture (strict/standard/degraded) is traceable, reviewable, and switchable without code edits | Done | Added `vmodel/project/live_probe_policy_profiles.yaml` with named profiles (`strict`, `standard`, `degraded`); extended `src/siasa/readmodels/live_probe_policy_gate.py` with `load_live_probe_policy_profile(policy_file, profile)`; upgraded `scripts/ci_live_probe_digest_gate_check.py` to resolve policy from file/profile with optional CLI overrides; updated workflow live-lane gate step to use `--policy-file ... --policy-profile standard` instead of hardcoded threshold literals; extended unit tests in `tests/unit/test_live_probe_policy_gate.py` for profile loading and unknown-profile failure; full suite verified green (`493 passed`). | Establishes policy-as-artifact governance for the live lane: threshold sets are now explicit repo-controlled configuration, improving auditability/change control while preserving deterministic CI pass/fail behavior |
| AP-INT-13 digest policy provenance + gate-result artifact | Make the active live-probe governance profile explicitly visible in digest evidence and persist machine-readable gate evaluation as an artifact, so auditors can trace both policy source and pass/fail rationale from one artifact set | Done | Extended `src/siasa/readmodels/live_probe_evidence_digest.py` to include `ci_context.live_probe_policy_file` and `ci_context.live_probe_policy_profile` from CI env. Updated workflow live lane to inject these env vars into digest build step and to persist gate evaluation JSON via `scripts/ci_live_probe_digest_gate_check.py --output build/run_artifacts/latest/readmodels/live_probe_policy_gate_result.json`; uploaded this gate-result JSON with diagnostics artifacts. Added digest regression test in `tests/unit/test_live_probe_evidence_digest.py` validating policy-context capture. | Closes governance traceability gap by binding each live-probe digest to an explicit policy profile and preserving deterministic gate rationale as a first-class evidence artifact |
| AP-INT-14 workflow-selectable live-probe governance profiles + resolved policy evidence | Allow operators to choose strict/standard/degraded governance profile at dispatch time and record resolved threshold values inside digest evidence for audit-grade reproducibility | Done | Extended `.github/workflows/vmodel-ci.yml` `workflow_dispatch` with `live_probe_policy_profile` choice input (`strict`,`standard`,`degraded`), wired into digest build env and gate check invocation. Extended `src/siasa/readmodels/live_probe_evidence_digest.py` to resolve active policy via `load_live_probe_policy_profile(...)` and emit `ci_context.live_probe_policy_resolved` plus `live_probe_policy_resolution_error`. Extended digest tests (`tests/unit/test_live_probe_evidence_digest.py`) to verify successful resolved-policy capture and explicit error surfacing for unknown profile. | Enables larger autonomous operations by decoupling governance posture selection from code edits while preserving deterministic, self-describing evidence of the exact thresholds used per live probe run |
|
---

## 4. Project-lead reading guide

If you want to know:

### What is implemented?
Look for capability status `Done` and confirm that the evidence columns point to:
- generated GUI/runtime outputs
- tests
- a completed work package / commit

### What is only partly begun?
Look for `Partial` or `Weak`:
- `Partial` = meaningful capability exists, but a stakeholder would still hit a practical limit
- `Weak` = mostly structure/preparation, not robust practical use

### What is still completely open?
Look for `Missing`.
In the current matrix, very few top-level areas are truly blank; most remaining gaps are now `Partial` rather than `Missing`.
That is a useful sign of progress, but it also means prioritization must focus on the highest-value remaining deltas, not on creating more thin surface area.

---

## 5. Re-Assessment: historical gap snapshot vs current closure state

> This section is intentionally kept as a historical marker and was re-synchronized after AP-F01..AP-F27 closure.
> The former 2026-05-30 "critical gaps" list is now obsolete for current repository truth.

### 5a. Historical snapshot status

The previously listed critical gaps (missing Domain C/E, no persistent latest cycle, no scheduler/monitoring,
missing fusion/probabilistic/epidemiology modules, and no historical persistence) are now closed by completed
work packages AP-F01..AP-F27 and corresponding evidence rows in section 3.

Operational note:
- Keep this section concise and non-contradictory with section 2/3.
- New substantive gaps should be recorded only when backed by current repo/runtime evidence.

### 5b. Current strategic next-step framing

With AP-F01..AP-F27 completed and capability matrix fulfillment at 100% on the current weighted model,
the next serial packages should be selected from one of these expansion tracks:

1) Runtime/source breadth expansion (new countries/sources with governed readiness evidence)
2) Validation realism depth (broader non-perfect historical replay portfolios)
3) Release/demo operability hardening (operator drill/action closure loops)
4) Architecture uplift beyond static/local GUI baseline (if explicitly prioritized)

Decision rule for next package:
- Prefer the track with highest stakeholder-value delta per implementation risk,
  while preserving strict serial package closure (implement + tests + docs + commit + push).
